{
  "about": "Security guards for scripts/mutation-check.mjs. Each entry removes or weakens one guard; the test suite must fail for every one. When a refactor moves a guard, update its find/replace here rather than deleting the entry.",
  "slowTests": ["test/cli.test.mjs"],
  "mutants": [
    { "id": "gate-grantor-did", "file": "src/gate.mjs", "guard": "a grant's grantor DID must name its publisher",
      "find": " && agentAddress(g.grantor) === a", "replace": "" },
    { "id": "gate-grant-iri-address", "file": "src/gate.mjs", "guard": "a grant id must embed its publisher's address",
      "find": " && grantIriAddress(g.id) === a", "replace": "" },
    { "id": "gate-subject-address", "file": "src/gate.mjs", "guard": "a grant's subject must belong to its publisher",
      "find": "subjectAddress(g.subject) === a && ", "replace": "" },
    { "id": "gate-vm-tier-only", "file": "src/gate.mjs", "guard": "only Verifiable Memory grants are authentic",
      "find": "if (!g || g.tier !== 'vm') return false", "replace": "if (!g) return false" },
    { "id": "gate-active-state-counted", "file": "src/gate.mjs", "guard": "an active state is not a revocation",
      "find": "const revokes = s.state !== 'active' || s.malformed === true", "replace": "const revokes = true" },
    { "id": "gate-context-tier-revocation", "file": "src/gate.mjs", "guard": "a merged-view revocation with no publisher is honoured",
      "find": " || s.tier === 'context')", "replace": ")" },
    { "id": "gate-revocation-publisher", "file": "src/gate.mjs", "guard": "a vm revocation counts only from the grant's publisher",
      "find": "s.publisher.toLowerCase() === grant.publisher.toLowerCase()", "replace": "true" },
    { "id": "gate-untrusted-spend", "file": "src/gate.mjs", "guard": "prior spend comes only from trusted producers",
      "find": "filter(d => d?.trusted === true && d.authorizedUnder === grantId)", "replace": "filter(d => d?.authorizedUnder === grantId)" },
    { "id": "gate-unreadable-trusted-spend", "file": "src/gate.mjs", "guard": "an unreadable trusted record under the grant makes spend unknown",
      "find": "const unknown = unreadable > 0 || under.some(", "replace": "const unknown = under.some(" },
    { "id": "gate-missing-billed-spend", "file": "src/gate.mjs", "guard": "a trusted edge with no billed amount makes spend unknown",
      "find": "const unknown = unreadable > 0 || under.some(d => !finiteNonNegative(d.billedUsd))", "replace": "const unknown = unreadable > 0" },
    { "id": "gate-valid-from", "file": "src/gate.mjs", "guard": "a grant is not used before validFrom",
      "find": "if (when < from) {", "replace": "if (false) {" },
    { "id": "gate-valid-until", "file": "src/gate.mjs", "guard": "a grant is not used after validUntil",
      "find": "if (when > until) {", "replace": "if (false) {" },
    { "id": "gate-unreadable-window", "file": "src/gate.mjs", "guard": "an unreadable validity window refuses",
      "find": "if (Number.isNaN(from) || Number.isNaN(until)) {\n      fail(4", "replace": "if (false) {\n      fail(4" },
    { "id": "gate-ignore-prior-spend", "file": "src/gate.mjs", "guard": "the ceiling includes spend already committed",
      "find": "prior.micro + estimateMicro > microUsd(ceiling, { down: true })", "replace": "estimateMicro > microUsd(ceiling, { down: true })" },
    { "id": "gate-ceiling-boundary", "file": "src/gate.mjs", "guard": "a render exactly at the ceiling is permitted, one over is not",
      "find": "prior.micro + estimateMicro > microUsd(ceiling, { down: true })", "replace": "prior.micro + estimateMicro >= microUsd(ceiling, { down: true })" },
    { "id": "gate-unknown-spend", "file": "src/gate.mjs", "guard": "an unknown spend under a ceiling refuses",
      "find": "if (spend.unknown) {", "replace": "if (false) {" },
    { "id": "gate-unreadable-ceiling", "file": "src/gate.mjs", "guard": "an unreadable ceiling refuses",
      "find": "if (!finiteNonNegative(ceiling)) { fail(6", "replace": "if (false) { fail(6" },
    { "id": "gate-micro-round-up", "file": "src/gate.mjs", "guard": "sub-micro-dollar amounts round up, so tiny renders cannot slip under a ceiling",
      "find": "if (!down && diff > tol) return m + 1n", "replace": "" },
    { "id": "gate-missing-consistency", "file": "src/gate.mjs", "guard": "knowledge without a consistent read refuses",
      "find": "if (k.consistency?.ok !== true)", "replace": "if (k.consistency?.ok === false)" },
    { "id": "gate-duplicate-grant-id", "file": "src/gate.mjs", "guard": "a grant id published twice is refused",
      "find": "if (copies.get(g.id) > 1) {", "replace": "if (false) {" },
    { "id": "gate-clause-list-strings", "file": "src/gate.mjs", "guard": "clause lists must be arrays, never substring-matched strings",
      "find": "Array.isArray(v) ? v : null)", "replace": "v)" },
    { "id": "gate-capability", "file": "src/gate.mjs", "guard": "the capability must be permitted",
      "find": "if (!Array.isArray(g.permitsCapability) || !g.permitsCapability.includes(capability)) {", "replace": "if (false) {" },
    { "id": "gate-forbidden-use-class", "file": "src/gate.mjs", "guard": "a forbidden use class refuses",
      "find": "if (forbids.includes(useClass)) {", "replace": "if (false) {" },
    { "id": "gate-territory", "file": "src/gate.mjs", "guard": "the territory must be covered",
      "find": "if (territories.length && !territories.includes(territory)) {", "replace": "if (false) {" },
    { "id": "gate-prohibited-use-class", "file": "src/gate.mjs", "guard": "prohibited use classes refuse whatever a grant says",
      "find": "if (isProhibitedUseClass(request.useClass)) {", "replace": "if (false) {" },
    { "id": "gate-malformed-state-revokes", "file": "src/gate.mjs", "guard": "the grantor's malformed state counts as a revocation whatever it says",
      "find": " || s.malformed === true", "replace": "" },
    { "id": "gate-untiered-state-revokes", "file": "src/gate.mjs", "guard": "the grantor's state with no recognised tier counts as a revocation",
      "find": "if (byGrantor && !STATE_TIERS.has(s.tier)) return true", "replace": "" },
    { "id": "gate-forgeries-list", "file": "src/gate.mjs", "guard": "knowledge without a forgeries list refuses",
      "find": "['grants', 'states', 'derivations', 'forgeries']", "replace": "['grants', 'states', 'derivations']" },
    { "id": "gate-micro-positive-min", "file": "src/gate.mjs", "guard": "any positive amount counts as at least one micro-dollar",
      "find": "if (!down && m === 0n && usd > 0) return 1n", "replace": "" },
    { "id": "gate-micro-down-branch", "file": "src/gate.mjs", "guard": "a ceiling with sub-micro digits is rounded down",
      "find": "if (down && diff < -tol) return m - 1n", "replace": "" },
    { "id": "gate-ceiling-down", "file": "src/gate.mjs", "guard": "the ceiling is rounded down, not up",
      "find": "> microUsd(ceiling, { down: true })", "replace": "> microUsd(ceiling)" },
    { "id": "gate-dup-other-subject", "file": "src/gate.mjs", "guard": "a grant id published twice is refused even under different subjects",
      "find": "for (const g of knowledge.grants.filter(grantIsAuthentic)) copies", "replace": "for (const g of candidates) copies" },
    { "id": "policy-prohibited-words", "file": "src/policy.mjs", "guard": "a label is refused when any of its words is prohibited",
      "find": "if (forms(joined).some(f => PROHIBITED.has(f))) return true", "replace": "if (i === 0 && j === words.length - 1 && PROHIBITED.has(joined)) return true" },
    { "id": "policy-inflections", "file": "src/policy.mjs", "guard": "prohibited terms are matched through common inflections",
      "find": "      out.push(stem, `${stem}e`)", "replace": "" },
    { "id": "policy-joined-words", "file": "src/policy.mjs", "guard": "adjacent words written together are matched (x-rated, deep-fakes)",
      "find": "for (let j = i; j < words.length; j++) {", "replace": "for (let j = i; j < i + 1; j++) {" },
    { "id": "policy-digit-split", "file": "src/policy.mjs", "guard": "digits split words, so nsfw18 is refused",
      "find": ".split(/[^a-z]+/)", "replace": ".split(/-+/)" },
    { "id": "policy-new-terms", "file": "src/policy.mjs", "guard": "lewd, hentai, x-rated, fetish and onlyfans are prohibited",
      "find": "  'lewd', 'hentai', 'x-rated', 'fetish', 'onlyfans', 'smut', 'kink', 'camgirl',\n", "replace": "" },

    { "id": "prov-grant-id-mismatch", "file": "src/provenance.mjs", "guard": "a grant id naming another address is a forgery",
      "find": "if (grantIriAddress(g.id) !== g.publisher) {", "replace": "if (false) {" },
    { "id": "prov-grant-not-by-subject", "file": "src/provenance.mjs", "guard": "a grant for someone else's subject is a forgery",
      "find": "if (g.subjectAddress !== g.publisher) {", "replace": "if (false) {" },
    { "id": "prov-grantor-literal", "file": "src/provenance.mjs", "guard": "a grantor literal naming another address is a forgery",
      "find": "if (g.grantorAddress !== g.publisher) {", "replace": "if (false) {" },
    { "id": "prov-misplaced-grant", "file": "src/provenance.mjs", "guard": "a grant in a derivations graph is never accepted",
      "find": "if (role !== 'grants') { report('misplaced-grant'", "replace": "if (false) { report('misplaced-grant'" },
    { "id": "prov-state-not-by-grantor", "file": "src/provenance.mjs", "guard": "a state about another address's grant is a forgery",
      "find": "if (owner !== anchor.publisher) { report('state-not-by-grantor'", "replace": "if (false) { report('state-not-by-grantor'" },
    { "id": "prov-was-attributed-to", "file": "src/provenance.mjs", "guard": "wasAttributedTo must name the anchor's address",
      "find": "if (attributed.some(a => agentAddress(a) !== publisher)) {", "replace": "if (false) {" },
    { "id": "prov-unconfirmed-status", "file": "src/provenance.mjs", "guard": "only confirmed anchors count",
      "find": "if (statuses.length !== 1 || statuses[0] !== 'confirmed') {", "replace": "if (false) {" },
    { "id": "prov-assertion-graph", "file": "src/provenance.mjs", "guard": "an anchor's assertion graph must be the one its UAL derives",
      "find": "if (graphs.length !== 1 || graphs[0] !== expectedGraph) {", "replace": "if (false) {" },
    { "id": "prov-state-author", "file": "src/provenance.mjs", "guard": "a mismatched stateAuthor makes the state malformed (so revoked)",
      "find": "if (authorCell && agentAddress(stateAuthor) !== anchor.publisher) {", "replace": "if (false) {" },
    { "id": "prov-malformed-state-active", "file": "src/provenance.mjs", "guard": "the grantor's malformed state counts as revoked, never active",
      "find": "state: raw === 'active' && problems.length === 0 ? 'active' : 'revoked',", "replace": "state: raw === 'active' ? 'active' : 'revoked'," },
    { "id": "prov-unreadable-state-of", "file": "src/provenance.mjs", "guard": "a state with an unreadable stateOf makes the read inconsistent",
      "find": "if (stateOf === null) {", "replace": "if (false) {" },
    { "id": "prov-derivation-id-mismatch", "file": "src/provenance.mjs", "guard": "a derivation id must match its output hash",
      "find": "if (!d.id.startsWith(", "replace": "if (false && !d.id.startsWith(" },
    { "id": "prov-trusted-legacy-format", "file": "src/provenance.mjs", "guard": "a trusted producer's legacy-format record is a forgery, not a warning",
      "find": "if (isTrusted()) report('legacy-format'", "replace": "if (false) report('legacy-format'" },
    { "id": "prov-forgery-trusted-flag", "file": "src/provenance.mjs", "guard": "a forgery under a trusted producer's prefix is marked trusted",
      "find": "if (role === 'derivations') return trusted.includes(pathPublisher)", "replace": "if (role === 'derivations') return false" },
    { "id": "prov-derivation-trusted", "file": "src/provenance.mjs", "guard": "a derivation is trusted only under a trusted producer",
      "find": "trusted: trustedProducers.includes(anchor.publisher),", "replace": "trusted: true," },
    { "id": "prov-derived-at-required", "file": "src/provenance.mjs", "guard": "a derivation must carry derivedAt",
      "find": "derivedAt: single(obj, V.derivedAt, asIso),", "replace": "derivedAt: single(obj, V.derivedAt, asIso, { required: false }) ?? null," },
    { "id": "prov-invalid-cell-dropped", "file": "src/provenance.mjs", "guard": "an unparseable cell is kept as invalid, never silently dropped",
      "find": "const o = parseCell(row.o) ?? { type: 'invalid', value: '' }", "replace": "const o = parseCell(row.o) ?? { type: 'invalid', value: '' }\n    if (o.type === 'invalid') continue" },
    { "id": "prov-known-uals", "file": "src/provenance.mjs", "guard": "an anchor seen before must still be present",
      "find": "if (missing.length) return fail(", "replace": "if (false) return fail(" },
    { "id": "prov-triple-count", "file": "src/provenance.mjs", "guard": "each anchored graph returns its declared triple count",
      "find": "if (n !== a.publicTripleCount) return fail(", "replace": "if (false) return fail(" },
    { "id": "prov-graph-count", "file": "src/provenance.mjs", "guard": "the node's graph count must match the anchors",
      "find": "if (visibleGraphCount !== undefined\n", "replace": "if (false\n" },
    { "id": "prov-unanchored-graph", "file": "src/provenance.mjs", "guard": "a returned graph with no anchor makes the read inconsistent",
      "find": "if (!anchors.has(g) && !pendingGraphs.has(g)) return fail(", "replace": "if (false) return fail(" },
    { "id": "prov-state-before-grant", "file": "src/provenance.mjs", "guard": "contract 3: the grantor's own state also typed LikenessGrant...",
      "find": "if (role === 'grants' && (types.includes(V.GrantState) || ownState)) {", "replace": "if (role === 'grants' && !types.includes(V.LikenessGrant) && (types.includes(V.GrantState) || ownState)) {" },
    { "id": "prov-own-untyped-state", "file": "src/provenance.mjs", "guard": "contract 3: the grantor's own \"active\" co-typed Derivation, or with no GrantState type, counts as revoked",
      "find": "if (role === 'grants' && (types.includes(V.GrantState) || ownState)) {", "replace": "if (role === 'grants' && types.includes(V.GrantState)) {" },
    { "id": "prov-state-type-problems", "file": "src/provenance.mjs", "guard": "contract 3: the grantor's own state also typed LikenessGrant is a revoked state entry...",
      "find": "const st = buildState(obj, anchor, stateOf, typeProblems)", "replace": "const st = buildState(obj, anchor, stateOf)" },
    { "id": "prov-trusted-untyped-kept", "file": "src/provenance.mjs", "guard": "item 5: a trusted producer's edge with no rdf:type ...",
      "find": " && !(mandatePredicates && isTrusted())) continue", "replace": ") continue" },
    { "id": "prov-trusted-unrecognised-type", "file": "src/provenance.mjs", "guard": "item 5: a trusted producer's edge with a foreign namespace type / an unknown mandate type ...",
      "find": "if (!refusalOnly) report('malformed'", "replace": "if (false) report('malformed'" },
    { "id": "prov-refusal-with-claims", "file": "src/provenance.mjs", "guard": "item 5: a trusted producer's edge with a Refusal type is a trusted malformed forgery carrying its grant and bill",
      "find": "&& !claims.outputSha256.length && !claims.authorizedUnder.length && !claims.billedUsd.length", "replace": "" },
    { "id": "prov-claims-uncapped", "file": "src/provenance.mjs", "guard": "claims keep every value: a trusted record naming the grant fifth still names it",
      "find": ".filter(v => typeof v === 'string'))]", "replace": ".filter(v => typeof v === 'string'))].slice(0, 4)" },
    { "id": "prov-unreadable-stateof-forgery", "file": "src/provenance.mjs", "guard": "contract 6: a trusted state with an unreadable stateOf is reported as a trusted malformed forgery with its claims",
      "find": "          report('malformed', 'unreadable stateOf')\n", "replace": "" },
    { "id": "prov-visible-over-anchored", "file": "src/provenance.mjs", "guard": "the graph count guard: more graphs visible than anchored plus pending is inconsistent",
      "find": " || visibleGraphCount > under.length + pendingUnder.length", "replace": "" },

    { "id": "resolve-freshness-status-0", "file": "src/resolve.mjs", "guard": "an unreachable freshness check fails the read",
      "find": "if (e.status === 403) warnings.push(", "replace": "if (e.status === 403 || e.status === 0) warnings.push(" },
    { "id": "resolve-freshness-http-errors", "file": "src/resolve.mjs", "guard": "404/409/429/5xx from reconcile fail the read",
      "find": "if (e.status === 403) warnings.push(", "replace": "if (e.status !== 0) warnings.push(" },
    { "id": "resolve-stale-only-warns", "file": "src/resolve.mjs", "guard": "a stale node fails the read",
      "find": "failures.push(`stale view:", "replace": "warnings.push(`stale view:" },
    { "id": "resolve-watermark-ahead", "file": "src/resolve.mjs", "guard": "a watermark ahead of the head fails the read",
      "find": "} else if (have > head || r?.status === 'watermark-ahead') {", "replace": "} else if (false) {" },
    { "id": "resolve-freshness-malformed", "file": "src/resolve.mjs", "guard": "a head or watermark that is not a number fails the read",
      "find": "if (head === null || have === null) {", "replace": "if (false) {" },
    { "id": "resolve-no-reconcile", "file": "src/resolve.mjs", "guard": "a node that cannot check freshness fails when the check is asked for",
      "find": "failures.push(`freshness of ${cg} cannot be checked", "replace": "warnings.push(`freshness of ${cg} cannot be checked" },
    { "id": "resolve-state-missing-from-read", "file": "src/resolve.mjs", "guard": "a grantor state visible in discovery must be in the grantor read",
      "find": "if (!inRead) failures.push(", "replace": "if (false) failures.push(" },
    { "id": "resolve-missing-graph-count", "file": "src/resolve.mjs", "guard": "a read with no graph count is inconsistent",
      "find": "if (visible < 0 && consistency.ok) Object.assign(", "replace": "if (false) Object.assign(" },
    { "id": "resolve-truncated-discovery", "file": "src/resolve.mjs", "guard": "a truncated discovery query fails the read",
      "find": "else if (e instanceof ReadTruncatedError) failures.push(`a discovery query", "replace": "else if (e instanceof ReadTruncatedError) warnings.push(`a discovery query" },
    { "id": "resolve-revocation-memory", "file": "src/resolve.mjs", "guard": "a revocation seen before is never forgotten",
      "find": "if (r && !states.some(", "replace": "if (false && !states.some(" },
    { "id": "resolve-empty-after-errors", "file": "src/resolve.mjs", "guard": "an empty read is believed only when every attempt answered",
      "find": "if (failed && anchors.size === 0) {", "replace": "if (false) {" },
    { "id": "resolve-pending-anchor", "file": "src/resolve.mjs", "guard": "an unconfirmed anchor in a deciding prefix makes the read inconsistent",
      "find": "if (problems.length) fail(", "replace": "if (false) fail(" },
    { "id": "resolve-retry-pending-anchor", "file": "src/resolve.mjs", "guard": "a read with anchor problems is retried",
      "find": "if (consistency.ok && anchors.size > 0 && problems.length === 0) break", "replace": "if (consistency.ok && anchors.size > 0) break" },
    { "id": "resolve-unreadable-objects", "file": "src/resolve.mjs", "guard": "unreadable objects in a deciding prefix make the read inconsistent",
      "find": "if (slice.unreadable.length) fail(", "replace": "if (false) fail(" },
    { "id": "resolve-paging", "file": "src/resolve.mjs", "guard": "a prefix read continues past its first page",
      "find": "if (page.length <= max) return rows", "replace": "return rows" },
    { "id": "resolve-discovery-failures-warn", "file": "src/resolve.mjs", "guard": "state discovery failures fail the read",
      "find": "failures.push(...f)", "replace": "warnings.push(...f)" },
    { "id": "resolve-discovery-unanswered", "file": "src/resolve.mjs", "guard": "a discovery query that never answers fails the read",
      "find": "if (answeredOnce.size !== queries.length) failures.push(", "replace": "if (false) failures.push(" },
    { "id": "resolve-merged-view-omitted", "file": "src/resolve.mjs", "guard": "a merged view left out of every answer fails the read",
      "find": "for (const cg of viewMissing) failures.push(", "replace": "for (const cg of []) failures.push(" },
    { "id": "resolve-discovery-batches", "file": "src/resolve.mjs", "guard": "state discovery covers every grant id, not the first 50",
      "find": ".flatMap(q => chunks(grantIds).map(batch => ({ ...q, batch })))", "replace": ".map(q => ({ ...q, batch: grantIds.slice(0, 50) }))" },
    { "id": "resolve-merged-view-revocation", "file": "src/resolve.mjs", "guard": "a revocation only in the merged view is honoured",
      "find": "} else if (inGrants && nonActive.has(key) && !vmIds.has(row.s)) {", "replace": "} else if (false) {" },
    { "id": "resolve-grant-discovery-failure", "file": "src/resolve.mjs", "guard": "grant discovery that does not answer fails the read",
      "find": "if (!got) throw new DiscoveryFailed(`grant discovery", "replace": "if (!got) continue\n    if (false) throw new DiscoveryFailed(`grant discovery" },
    { "id": "resolve-not-subscribed", "file": "src/resolve.mjs", "guard": "a graph the node does not hold (or holds in another case) fails the read",
      "find": "failures.push(...held.failures)", "replace": "warnings.push(...held.failures)" },
    { "id": "resolve-unresolved-grants", "file": "src/resolve.mjs", "guard": "grants in graphs this reader does not read are reported unresolved",
      "find": ".filter(id => { const o = grantIriAddress(id); return Boolean(o) && !grantsOwners.has(o) && !readGrantIds.has(id) })", "replace": ".filter(() => false)" },

    { "id": "verify-derived-before-from", "file": "src/verify-core.mjs", "guard": "a render recorded before validFrom is TAINTED",
      "find": " || derived < from", "replace": "" },
    { "id": "verify-derived-after-until", "file": "src/verify-core.mjs", "guard": "a render recorded after expiry is TAINTED / UNAUTHORISED",
      "find": "if (derived > until) {", "replace": "if (false) {" },
    { "id": "verify-expired", "file": "src/verify-core.mjs", "guard": "an expired grant is TAINTED / EXPIRED",
      "find": "if (nowMs > until) {", "replace": "if (false) {" },
    { "id": "verify-revoked", "file": "src/verify-core.mjs", "guard": "a revoked grant is TAINTED / REVOKED",
      "find": "if (rev.revoked) {", "replace": "if (false) {" },
    { "id": "verify-capability", "file": "src/verify-core.mjs", "guard": "a capability the grant never permitted is TAINTED",
      "find": "if (!grant.permitsCapability.includes(d.servedCapability)) {", "replace": "if (false) {" },
    { "id": "verify-duplicate-grant", "file": "src/verify-core.mjs", "guard": "a grant id published twice is TAINTED / MALFORMED",
      "find": "if (found.length > 1) return out(", "replace": "if (false) return out(" },
    { "id": "verify-unreadable-derived-at", "file": "src/verify-core.mjs", "guard": "a trusted edge with no readable render time is MALFORMED",
      "find": "if (Number.isNaN(derived)) {", "replace": "if (false) {" },
    { "id": "verify-unreadable-window", "file": "src/verify-core.mjs", "guard": "an unreadable validity window is MALFORMED",
      "find": "if (Number.isNaN(from) || Number.isNaN(until)) {", "replace": "if (false) {" },
    { "id": "verify-untrusted-edges", "file": "src/verify-core.mjs", "guard": "untrusted edges are shown, never believed",
      "find": "const trusted = edges.filter(d => d.trusted === true)", "replace": "const trusted = edges" },
    { "id": "verify-trusted-forgery", "file": "src/verify-core.mjs", "guard": "a trusted producer's unreadable record for these bytes is TAINTED",
      "find": "if (!isTrusted) continue", "replace": "continue" },
    { "id": "verify-unresolved-unknown", "file": "src/verify-core.mjs", "guard": "an edge citing a grant in an unread graph is UNKNOWN, not TAINTED",
      "find": "    if (unread) {\n      return out(UNKNOWN", "replace": "    if (false) {\n      return out(UNKNOWN" },
    { "id": "verify-tainted-precedence", "file": "src/verify-core.mjs", "guard": "any TAINTED judgement taints the file",
      "find": "const verdict = tainted.length ? TAINTED : unknown.length ? UNKNOWN : CLEAR", "replace": "const verdict = unknown.length ? UNKNOWN : tainted.length ? TAINTED : CLEAR" },
    { "id": "verify-inconsistent", "file": "src/verify-core.mjs", "guard": "knowledge without a consistent read is INCONCLUSIVE",
      "find": "if (k?.consistency?.ok !== true) {", "replace": "if (k?.consistency?.ok === false) {" },
    { "id": "verify-missing-lists", "file": "src/verify-core.mjs", "guard": "hand-built knowledge missing a list is INCONCLUSIVE",
      "find": "if (!Array.isArray(k[f])) return { ...base, verdict: INCONCLUSIVE", "replace": "if (false) return { ...base, verdict: INCONCLUSIVE" },
    { "id": "verify-blast-unreadable", "file": "src/verify-core.mjs", "guard": "unreadable trusted records make the blast radius's billing unknown",
      "find": "const unknown = unreadable > 0 || assets.some(d => !readable(d))", "replace": "const unknown = assets.some(d => !readable(d))" },
    { "id": "verify-read-grant-judged-first", "file": "src/verify-core.mjs", "guard": "a grant that was read is judged, and its revocation checked, before the unresolved UNKNOWN",
      "find": "  const unread = unresolved.has(d.authorizedUnder)\n", "replace": "  const unread = unresolved.has(d.authorizedUnder)\n  if (unread) return out(UNKNOWN, null, 'unresolved')\n" },
    { "id": "verify-unresolved-never-clear", "file": "src/verify-core.mjs", "guard": "a live grant whose owner's graph is unread is UNKNOWN, never CLEAR",
      "find": "  if (unread) {\n    return out(UNKNOWN", "replace": "  if (false) {\n    return out(UNKNOWN" },
    { "id": "verify-forgeries-list", "file": "src/verify-core.mjs", "guard": "knowledge without a forgeries list is INCONCLUSIVE",
      "find": "['grants', 'states', 'derivations', 'forgeries']", "replace": "['grants', 'states', 'derivations']" },
    { "id": "verify-edge-sha-case", "file": "src/verify-core.mjs", "guard": "edge hashes are compared case-insensitively",
      "find": "filter(d => normSha256(d?.outputSha256) === sha)", "replace": "filter(d => d?.outputSha256 === sha)" },
    { "id": "verify-now-before-from", "file": "src/verify-core.mjs", "guard": "a grant not yet valid now is NOT_YET_VALID",
      "find": "nowMs < from || ", "replace": "" },
    { "id": "verify-severity-sort", "file": "src/verify-core.mjs", "guard": "the headline is the most serious sub-status",
      "find": "\n    .sort((a, b) => SEVERITY.indexOf(a.subStatus) - SEVERITY.indexOf(b.subStatus))", "replace": "" },
    { "id": "verify-severity-order", "file": "src/verify-core.mjs", "guard": "REVOKED outranks EXPIRED",
      "find": "const SEVERITY = ['REVOKED', 'UNAUTHORISED', 'MALFORMED', 'NOT_YET_VALID', 'EXPIRED']", "replace": "const SEVERITY = ['EXPIRED', 'NOT_YET_VALID', 'MALFORMED', 'UNAUTHORISED', 'REVOKED']" },
    { "id": "verify-legacy-trust-fallback", "file": "src/verify-core.mjs", "guard": "a malformed record from a trusted address without a trusted flag taints",
      "find": " || (f.trusted === undefined && f.kind === 'malformed' && trustedProducers.has(lower(f.publisher)))", "replace": "" },

    { "id": "rdf-term-calendar", "file": "src/rdf-term.mjs", "guard": "calendar-impossible dates are refused, not rolled forward",
      "find": "if (!days || day < 1 || day > days || hour > 23 || minute > 59 || second > 59 || offH > 14 || offM > 59) return NaN", "replace": "" },
    { "id": "rdf-term-decimal-round-up", "file": "src/rdf-term.mjs", "guard": "a positive amount is never written as 0",
      "find": "if (shortfall > Math.max(1e-12, n * 4 * Number.EPSILON)) {", "replace": "if (false) {" },
    { "id": "rdf-term-decimal-positive-min", "file": "src/rdf-term.mjs", "guard": "a positive amount too small for a micro-dollar digit is written as 0.000001",
      "find": "  if (n > 0 && Number(s) === 0) s = '0.000001'\n", "replace": "" },
    { "id": "rdf-term-offset-range", "file": "src/rdf-term.mjs", "guard": "offsets beyond 14:00 are refused",
      "find": "  if (offH * 60 + offM > 840) return NaN\n", "replace": "" },
    { "id": "rdf-term-year-range", "file": "src/rdf-term.mjs", "guard": "an instant outside years 0000-9999 UTC is refused by the reader",
      "find": "  if (!(ms >= MIN_DATETIME_MS && ms <= MAX_DATETIME_MS)) return NaN\n", "replace": "" },
    { "id": "rdf-term-writer-roundtrip", "file": "src/rdf-term.mjs", "guard": "the dateTime writer refuses output its reader would reject",
      "find": "if (iso === null || asDateTime(iso) !== ms) {", "replace": "if (iso === null) {" },

    { "id": "scope-negator-words", "file": "src/scope.mjs", "guard": "a negator anywhere in a clause contradicts its terms",
      "find": "return words(t).some(w => NEGATOR_WORDS.has(w) || REFUSAL_STEMS.some(s => w.startsWith(s))) || NEGATOR_PHRASES.some(p => has(t, p))", "replace": "return NEGATOR_PHRASES.some(p => has(t, p))" },
    { "id": "scope-negator-phrases", "file": "src/scope.mjs", "guard": "phrases such as 'other than' and 'off limits' negate",
      "find": " || NEGATOR_PHRASES.some(p => has(t, p))", "replace": "" },
    { "id": "scope-guard-phrase-scope", "file": "src/scope.mjs", "guard": "a guard phrase removes only its own words ('I don't mind no ads')",
      "find": "for (const g of NOT_NEGATING) t = t.split(` ${g} `).join(' ')", "replace": "for (const g of NOT_NEGATING) if (has(t, g)) return ''" },
    { "id": "scope-refusal-verbs", "file": "src/scope.mjs", "guard": "a refusal verb anywhere contradicts consent",
      "find": "if (hasRefusal(norm)) {", "replace": "if (false) {" },
    { "id": "scope-retractions", "file": "src/scope.mjs", "guard": "a retraction contradicts consent",
      "find": "if (RETRACTIONS.some(r => has(unguardedAll, r))) {", "replace": "if (false) {" },
    { "id": "scope-negated-consent", "file": "src/scope.mjs", "guard": "consent said in a negated clause is contradicted",
      "find": "if (cl.negated && cl.consentWord) {", "replace": "if (false) {" },
    { "id": "scope-bare-refusal", "file": "src/scope.mjs", "guard": "a bare 'No.' answers the clause before it",
      "find": "if (cl.negated && (!cl.aboutSomething || words(cl.text).some(w => ADDITIVE.has(w)))) {", "replace": "if (false) {" },
    { "id": "scope-negated-term", "file": "src/scope.mjs", "guard": "a term in a refusing clause is contradicted",
      "find": "if (clauseRefuses(m.ci)) { contradicted = true; continue }", "replace": "if (false) { contradicted = true; continue }" },
    { "id": "scope-hedged-consent", "file": "src/scope.mjs", "guard": "questions, conditionals and reported speech are not consent",
      "find": "if (sentences[cl.sentence].hedged) {", "replace": "if (false) {" },
    { "id": "scope-hedged-term", "file": "src/scope.mjs", "guard": "a term only said in a hedged sentence is missing",
      "find": "if (sentences[clauses[m.ci].sentence].hedged) {", "replace": "if (false) {" },
    { "id": "scope-first-person", "file": "src/scope.mjs", "guard": "consent must be first person",
      "find": "if (w[i] !== 'i' && w[i] !== 'we') continue", "replace": "" },
    { "id": "scope-ambiguous-alias", "file": "src/scope.mjs", "guard": "one stretch of speech never satisfies two terms",
      "find": "if (m.ambiguous) {", "replace": "if (false) {" },
    { "id": "scope-territory-compound", "file": "src/scope.mjs", "guard": "a place inside a longer place name is not that place",
      "find": "if (TERRITORY_BLOCKED_BEFORE.has(before) || TERRITORY_BLOCKED_AFTER.has(after)) ok = false", "replace": "" },
    { "id": "scope-unchecked", "file": "src/scope.mjs", "guard": "validity and ceiling are listed as unchecked",
      "find": "const unchecked = ['validity', 'ceiling']", "replace": "const unchecked = []" },
    { "id": "scope-script-closed-world", "file": "src/scope.mjs", "guard": "only a reading of the script is confirmed",
      "find": "const confirmed = scriptMatch.matched && contradicted.length === 0", "replace": "const confirmed = contradicted.length === 0" },
    { "id": "scope-script-contradiction-wins", "file": "src/scope.mjs", "guard": "a contradicted script reading is not confirmed",
      "find": "const confirmed = scriptMatch.matched && contradicted.length === 0", "replace": "const confirmed = scriptMatch.matched" },
    { "id": "scope-script-extra-words", "file": "src/scope.mjs", "guard": "every unaligned transcript word must be filler",
      "find": "const meaningful = extra.filter(w => !SCRIPT_FILLER.has(w))", "replace": "const meaningful = []" },
    { "id": "scope-script-critical", "file": "src/scope.mjs", "guard": "a critical script word never goes missing",
      "find": " && !missing.some(x => x.critical)", "replace": "" },
    { "id": "scope-script-max-misses", "file": "src/scope.mjs", "guard": "at most SCRIPT_MAX_MISSES non-critical words may be missing",
      "find": " && missing.length <= SCRIPT_MAX_MISSES", "replace": "" },
    { "id": "scope-script-length-cap", "file": "src/scope.mjs", "guard": "a transcript far longer than the script is never a match",
      "find": "if (m > 4 * n + 64) return", "replace": "if (false) return" },
    { "id": "scope-negators-round2", "file": "src/scope.mjs", "guard": "unacceptable, dealbreaker, nay, negative, scrap, pass negate",
      "find": "'unacceptable', 'dealbreaker', 'nay', 'negative', 'scrap', 'pass',", "replace": "" },
    { "id": "scope-negators-no-apostrophe", "file": "src/scope.mjs", "guard": "havent, mustnt and similar negate",
      "find": "'havent', 'hasnt', 'hadnt', 'mustnt', 'neednt', 'shant', 'mightnt', ", "replace": "" },
    { "id": "scope-exclusion-words", "file": "src/scope.mjs", "guard": "save, bar, barring, minus, beyond exclude",
      "find": "'save', 'bar', 'barring', 'minus', 'sans', 'beyond', 'excepted',", "replace": "" },
    { "id": "scope-negator-phrases-round2", "file": "src/scope.mjs", "guard": "out of the question, leave out and similar negate",
      "find": ", 'out of the question', 'else than', 'draw the line', 'away from', 'leave out', 'no go', 'hard no', 'right out'", "replace": "" },
    { "id": "scope-refusal-retract-rescind", "file": "src/scope.mjs", "guard": "retract and rescind refuse consent",
      "find": ", 'retract', 'rescind']", "replace": "]" },
    { "id": "scope-retractions-round2", "file": "src/scope.mjs", "guard": "that was a lie, take back, pigs fly retract",
      "find": "  'take back', 'strike that', 'delete that', 'undo', 'lie', 'lies',", "replace": "  " },
    { "id": "scope-coercion", "file": "src/scope.mjs", "guard": "coercion said anywhere contradicts consent",
      "find": "  'forced', 'duress', 'coerced', 'threatened', 'made me', 'told me to', 'repeat after']", "replace": "  ]" },
    { "id": "scope-not-only-negates", "file": "src/scope.mjs", "guard": "'not only' does not swallow a negator",
      "find": "const NOT_NEGATING = ['would not mind',", "replace": "const NOT_NEGATING = ['not only', 'would not mind'," },
    { "id": "scope-hedge-words-round2", "file": "src/scope.mjs", "guard": "wrote, read, repeat, when, after hedge",
      "find": "  'wrote', 'writes', 'written', 'stated', 'read', 'reads', 'reading', 'script', 'repeat', 'conditional', 'conditionally', 'when', 'whenever', 'after', 'pending'])", "replace": "  ])" },
    { "id": "scope-hedge-phrases-round2", "file": "src/scope.mjs", "guard": "on the condition, subject to, states that hedge",
      "find": " 'on the condition', 'subject to', 'in return for', 'in exchange for', 'states that', 'states i', 'states we',", "replace": "" },
    { "id": "scope-question-before-subject", "file": "src/scope.mjs", "guard": "an auxiliary or wh-word before I makes a question",
      "find": "    if (BEFORE_SUBJECT.has(w[i - 1])) continue\n", "replace": "" },
    { "id": "scope-agree-to", "file": "src/scope.mjs", "guard": "agree and consent only with to, for or at clause end",
      "find": "(w[j + 1] === undefined || w[j + 1] === 'to' || w[j + 1] === 'for')", "replace": "true" },
    { "id": "scope-allow-delegation", "file": "src/scope.mjs", "guard": "'allow my lawyer to' is not consent",
      "find": "(w[j + 1] === 'you' || !w.slice(j + 1, j + 4).includes('to'))", "replace": "true" },
    { "id": "scope-universal-but-any-distance", "file": "src/scope.mjs", "guard": "a but after a universal excludes at any distance",
      "find": "sentence.slice(at).replace(/ but (?=\\S)/g, ' except ')", "replace": "sentence.slice(at)" },
    { "id": "scope-universal-anything", "file": "src/scope.mjs", "guard": "'anything' is a universal",
      "find": "|anything|", "replace": "|" },
    { "id": "scope-bare-but-exclusion", "file": "src/scope.mjs", "guard": "'but <term>' with nothing else excludes",
      "find": "const bareExclusion = parts[p - 1] === 'but' && isOnlyTerms(text)", "replace": "const bareExclusion = false" },

    { "id": "consent-transcript-is-clip", "file": "src/consent.mjs", "guard": "the consent clip itself is never read as its transcript",
      "find": "if (sameResource(u.href, sourceUrl)) throw", "replace": "if (false) throw" },
    { "id": "consent-transcript-content-type", "file": "src/consent.mjs", "guard": "a transcript link must be text or JSON",
      "find": "if (!TEXT_TYPES.test(type)) {", "replace": "if (false) {" },
    { "id": "consent-text-mixed-with-link", "file": "src/consent.mjs", "guard": "transcript text mixed with a link is refused",
      "find": "if (transcript && hasLink(transcript) && !looksLikeUrlOnly(transcript)) throw", "replace": "if (false) throw" },
    { "id": "consent-failure-nested", "file": "src/consent.mjs", "guard": "failure markers inside result, output and run_output count",
      "find": "const layers = [reply, reply?.result, reply?.output, reply?.run_output]", "replace": "const layers = [reply]" },
    { "id": "consent-failure-success-false", "file": "src/consent.mjs", "guard": "success: false is a failure",
      "find": "if (o.ok === false || o.success === false)", "replace": "if (o.ok === false)" },
    { "id": "consent-failure-state-fields", "file": "src/consent.mjs", "guard": "state, job_status and phase are read like status",
      "find": "for (const field of ['status', 'state', 'job_status', 'phase'])", "replace": "for (const field of ['status'])" },
    { "id": "consent-asr-done-only", "file": "src/consent.mjs", "guard": "a status outside ASR_DONE is a failure",
      "find": "if (!ASR_DONE.has(status)) return", "replace": "if (false) return" },
    { "id": "consent-asr-ok-true", "file": "src/consent.mjs", "guard": "the ASR reply must say ok: true",
      "find": "if (s.ok !== true) throw fail", "replace": "if (false) throw fail" },
    { "id": "consent-asr-capability-named", "file": "src/consent.mjs", "guard": "the ASR reply must name its capability",
      "find": "if (s.capability == null && s.requested_capability == null) throw", "replace": "if (false) throw" },
    { "id": "consent-asr-source-named", "file": "src/consent.mjs", "guard": "the ASR reply must name the clip it heard",
      "find": "if (s.source_url == null && s.inputs?.audio_url == null) throw", "replace": "if (false) throw" },
    { "id": "consent-asr-structured-only", "file": "src/consent.mjs", "guard": "plain text alone is never a transcript",
      "find": "if (!s || typeof s !== 'object' || Array.isArray(s)) throw", "replace": "if (false) throw" },
    { "id": "consent-asr-loose-status-text", "file": "src/consent.mjs", "guard": "status text in top-level text or output is not speech",
      "find": "if (platformTextIn(s)) throw fail(", "replace": "if (false) throw fail(" },
    { "id": "consent-asr-status-text-no-person", "file": "src/consent.mjs", "guard": "status-like text with no first-person word is not speech",
      "find": "if (transcript && !looksLikeUrlOnly(transcript) && STATUS_TEXT.test(transcript) && !", "replace": "if (false && !" },
    { "id": "consent-link-https", "file": "src/consent.mjs", "guard": "a transcript link uses https unless loopback",
      "find": "if (u.protocol !== 'https:' && !(u.protocol === 'http:' && LOOPBACK.has(u.hostname))) throw", "replace": "if (u.protocol !== 'https:' && u.protocol !== 'http:') throw" },
    { "id": "consent-link-stream-error", "file": "src/consent.mjs", "guard": "a body stream error is a ConsentError at stage asr",
      "find": "    throw new ConsentError(`transcript link failed while reading: ${e?.message ?? e}`, { stage: 'asr' })", "replace": "    throw e" },
    { "id": "consent-link-json-failure", "file": "src/consent.mjs", "guard": "a JSON transcript link's own failure fields are honoured",
      "find": "if (failed) throw new ConsentError(`transcript link: ${failed}`, { stage: 'asr' })", "replace": "if (false) throw new ConsentError('', { stage: 'asr' })" },
    { "id": "consent-link-plain-status", "file": "src/consent.mjs", "guard": "a plain-text link body that reads like a status is refused",
      "find": "if (STATUS_TEXT.test(body)) throw", "replace": "if (false) throw" },
    { "id": "consent-link-nul-byte", "file": "src/consent.mjs", "guard": "a NUL byte in a transcript body is binary",
      "find": "if (body.includes('\\u0000')) throw", "replace": "if (false) throw" },
    { "id": "consent-clip-path-decoded", "file": "src/consent.mjs", "guard": "percent-encoding does not disguise the clip",
      "find": "try { p = decodeURIComponent(p) } catch", "replace": "try { p = String(p) } catch" },
    { "id": "consent-clip-path-slashes", "file": "src/consent.mjs", "guard": "doubled slashes do not disguise the clip",
      "find": ".replace(/\\/{2,}/g, '/')", "replace": "" },
    { "id": "consent-upload-tool-error-expired", "file": "src/consent.mjs", "guard": "a tool error saying expired ends the wait",
      "find": "if (saysExpired(said)) return { url: null, status: 'expired'", "replace": "if (false) return { url: null, status: 'expired'" },
    { "id": "consent-upload-tool-error-permanent", "file": "src/consent.mjs", "guard": "a permanent tool error ends the wait",
      "find": "if (PERMANENT_TOOL_ERROR.test(said)) return", "replace": "if (false) return" },
    { "id": "consent-upload-tool-error-repeated", "file": "src/consent.mjs", "guard": "repeated identical tool errors end the wait",
      "find": "if (sameToolError >= TOOL_ERROR_LIMIT) return", "replace": "if (false) return" },
    { "id": "consent-upload-unknown-status", "file": "src/consent.mjs", "guard": "an unknown upload status ends the wait as itself",
      "find": "if (!UPLOAD_WAITING.has(r.status)) return", "replace": "if (['expired', 'failed'].includes(r.status)) return" },

    { "id": "execute-input-echo", "file": "src/execute.mjs", "guard": "a render's own input is never released as its output",
      "find": " && !inputs.has(pathKey(n)) ? n : null", "replace": " ? n : null" },
    { "id": "execute-input-echo-query", "file": "src/execute.mjs", "guard": "an input echoed back with a query string is still the input",
      "find": "!inputs.has(pathKey(n))", "replace": "!inputs.has(n)" },
    { "id": "execute-nested-inputs", "file": "src/execute.mjs", "guard": "inputs nested inside objects are collected",
      "find": "for (const v of Array.isArray(value) ? value : Object.values(value)) collectInputUrls(v, out, seen)", "replace": "if (Array.isArray(value)) for (const v of value) collectInputUrls(v, out, seen)" },
    { "id": "execute-job-inputs", "file": "src/execute.mjs", "guard": "a polled job's own source and inputs are never its output",
      "find": "const inputs = [...collectInputUrls(inputUrls), ...collectInputUrls(s.source_url), ...collectInputUrls(s.inputs)]", "replace": "const inputs = [...collectInputUrls(inputUrls)]" },
    { "id": "execute-queued-text-scanned", "file": "src/execute.mjs", "guard": "a queued text-only reply is polled, never scanned for a URL",
      "find": "const queued = IN_PROGRESS.has(status) || (jobId != null && !inlineUrl)", "replace": "const queued = IN_PROGRESS.has(status)" },
    { "id": "execute-no-media", "file": "src/execute.mjs", "guard": "a finished job with no media URL is an error",
      "find": "if (!url) throw new RenderError(`job ${jobId} finished without a media URL`", "replace": "if (false) throw new RenderError(`job ${jobId} finished without a media URL`" },
    { "id": "execute-unknown-status", "file": "src/execute.mjs", "guard": "an unrecognised job status stops the render",
      "find": "if (!IN_PROGRESS.has(status)) {", "replace": "if (false) {" },
    { "id": "execute-timeout-may-have-started", "file": "src/execute.mjs", "guard": "a call that got no answer may still be rendering",
      "find": "{ kind: timedOut ? 'timeout' : 'tool', jobId: null, mayHaveStarted: true }", "replace": "{ kind: timedOut ? 'timeout' : 'tool', jobId: null, mayHaveStarted: false }" },
    { "id": "execute-job-id-shape", "file": "src/execute.mjs", "guard": "a malformed job id is refused",
      "find": "return typeof given === 'string' && JOB_ID.test(given) ? { jobId: given, malformed: false }", "replace": "return typeof given === 'string' ? { jobId: given, malformed: false }" },
    { "id": "execute-queued-structured-url", "file": "src/execute.mjs", "guard": "a reply with a job id and no done status is polled, even when it carries a structured url",
      "find": "const queued = IN_PROGRESS.has(status) || (jobId != null && !inlineUrl)", "replace": "const queued = IN_PROGRESS.has(status) || (jobId != null && !structured?.url)" },
    { "id": "execute-queued-inline-done-only", "file": "src/execute.mjs", "guard": "a reply with a job id and no done status is polled, even when it carries a structured url",
      "find": "const inlineUrl = DONE.has(status) ? extractMediaUrl(structured, '', inputUrls) : null", "replace": "const inlineUrl = extractMediaUrl(structured, '', inputUrls)" },
    { "id": "execute-queued-marker-no-id", "file": "src/execute.mjs", "guard": "a queued reply whose job id is not in the accepted shape stops instead of scanning the text",
      "find": "if (!jobId && QUEUED_TEXT.test(String(text ?? ''))) {", "replace": "if (false) {" },
    { "id": "execute-echo-scheme", "file": "src/execute.mjs", "guard": "an echoed input is recognised across scheme, percent-encoding, repeated slashes, host case and default port",
      "find": "return `${x.hostname.toLowerCase()}${port}${path.replace(/\\/{2,}/g, '/')}`", "replace": "return `${x.protocol}${x.hostname.toLowerCase()}${port}${path.replace(/\\/{2,}/g, '/')}`" },
    { "id": "execute-echo-percent", "file": "src/execute.mjs", "guard": "an echoed input is recognised across scheme, percent-encoding, repeated slashes, host case and default port",
      "find": "try { path = decodeURIComponent(path) } catch", "replace": "try { path = String(path) } catch" },
    { "id": "execute-echo-slashes", "file": "src/execute.mjs", "guard": "an echoed input is recognised across scheme, percent-encoding, repeated slashes, host case and default port",
      "find": "${path.replace(/\\/{2,}/g, '/')}", "replace": "${path}" },
    { "id": "execute-structured-url-no-text-fallback", "file": "src/execute.mjs", "guard": "a finished job whose structured url is an input is no-media, even with another media link in its text",
      "find": "  if (named) return null\n", "replace": "\n" },
    { "id": "execute-poll-iserror", "file": "src/execute.mjs", "guard": "pollJob never takes a reply marked isError as a result",
      "find": "if (r.isError || FAILED.has(status)) {", "replace": "if (FAILED.has(status) || (r.isError && status === undefined)) {" },
    { "id": "execute-poll-header-job", "file": "src/execute.mjs", "guard": "pollJob refuses a reply about another job, and reads multi-word text statuses",
      "find": "if (jobId && m[1].toLowerCase() !== jobId.toLowerCase()) {", "replace": "if (false) {" },
    { "id": "execute-poll-structured-job", "file": "src/execute.mjs", "guard": "pollJob refuses a reply about another job, and reads multi-word text statuses",
      "find": "if (s.job_id !== undefined && s.job_id !== null && (typeof s.job_id !== 'string' || s.job_id.toLowerCase() !== jobId.toLowerCase())) {", "replace": "if (false) {" },
    { "id": "execute-status-header-only", "file": "src/execute.mjs", "guard": "pollJob refuses a reply about another job, and reads multi-word text statuses",
      "find": "const m = String(text ?? '').match(STATUS_HEADER)", "replace": "const m = String(text ?? '').match(/(?:status=)()([A-Za-z_-]{1,40})/) ?? String(text ?? '').match(STATUS_HEADER)" },
    { "id": "execute-status-multiword", "file": "src/execute.mjs", "guard": "pollJob refuses a reply about another job, and reads multi-word text statuses",
      "find": "([A-Za-z][A-Za-z_ -]{0,39}?)[ \\t\\r]*(?:\\(|$)/m", "replace": "([A-Za-z_-]{1,40})\\b/m" },
    { "id": "execute-served-valid", "file": "src/execute.mjs", "guard": "the served capability and cost are always writable: anything else is null with a warning",
      "find": "if (typeof v === 'string' && CAPABILITY.test(v)) return { capability: v, warning: null }\n    return { capability: null, warning: `the platform named", "replace": "return { capability: v, warning: null }\n    return { capability: null, warning: `the platform named" },
    { "id": "execute-cost-writable", "file": "src/execute.mjs", "guard": "the served capability and cost are always writable: anything else is null with a warning",
      "find": "decimalTerm(v, 'cost_usd_estimated')", "replace": "void v" },
    { "id": "execute-payment-strip-urls", "file": "src/execute.mjs", "guard": "payment classification ignores URLs, input-fetch credential errors and token allowances",
      "find": "const words = String(text ?? '').replace(/\\b[a-z][a-z0-9+.-]*:\\/\\/\\S+/gi, ' ')", "replace": "const words = String(text ?? '')" },
    { "id": "execute-payment-401-input", "file": "src/execute.mjs", "guard": "payment classification ignores URLs, input-fetch credential errors and token allowances",
      "find": "if (code === '401') return inputFetch ? 'tool' : 'payment'", "replace": "if (code === '401') return 'payment'" },
    { "id": "execute-payment-numeric-other", "file": "src/execute.mjs", "guard": "payment classification ignores URLs, input-fetch credential errors and token allowances",
      "find": "    if (code === '401') return inputFetch ? 'tool' : 'payment'\n    return 'tool'", "replace": "    if (code === '401') return inputFetch ? 'tool' : 'payment'\n    return 'payment'" },
    { "id": "execute-payment-unauthorized-input", "file": "src/execute.mjs", "guard": "payment classification ignores URLs, input-fetch credential errors and token allowances",
      "find": "const soft = inputFetch ? words.replace(/\\bunauthori[sz]ed\\b/gi, ' ') : words", "replace": "const soft = words" },
    { "id": "execute-payment-generic-code-text", "file": "src/execute.mjs", "guard": "payment classification ignores URLs, input-fetch credential errors and token allowances",
      "find": "if (code) return STRONG_PAYMENT_TEXT.test(words) ? 'payment' : 'tool'", "replace": "if (code) return 'tool'" },
    { "id": "execute-payment-allowance", "file": "src/execute.mjs", "guard": "payment classification ignores URLs, input-fetch credential errors and token allowances",
      "find": "|\\b(?:credit|spend(?:ing)?|billing|usage) allowance\\b|", "replace": "|\\ballowance\\b|" },
    { "id": "execute-platform-timeout-recoverable", "file": "src/execute.mjs", "guard": "a platform error that says the render timed out or continues is kept recoverable; other errors are not",
      "find": "mayHaveStarted: jobId != null || malformed || stillRunning,", "replace": "mayHaveStarted: jobId != null || malformed," },
    { "id": "execute-platform-error-not-started", "file": "src/execute.mjs", "guard": "a platform error that says the render timed out or continues is kept recoverable; other errors are not",
      "find": "mayHaveStarted: jobId != null || malformed || stillRunning,", "replace": "mayHaveStarted: true," },
    { "id": "execute-timeout-cause-code", "file": "src/execute.mjs", "guard": "a platform error that says the render timed out or continues is kept recoverable; other errors are not",
      "find": " ${e?.cause?.code ?? ''} ${e?.cause?.name ?? ''}`)", "replace": "`)" },
    { "id": "execute-malformed-id-payment", "file": "src/execute.mjs", "guard": "a malformed job id on an error reply does not hide a payment refusal, and keeps it recoverable",
      "find": "return typeof given === 'string' && JOB_ID.test(given) ? { jobId: given, malformed: false } : { jobId: null, malformed: true }", "replace": "if (typeof given === 'string' && JOB_ID.test(given)) return { jobId: given, malformed: false }\n    throw new RenderError('job id not in the expected form', { kind: 'tool', structured, mayHaveStarted: true })" },
    { "id": "execute-poll-options-before-dispatch", "file": "src/execute.mjs", "guard": "invalid poll options fail before anything is dispatched",
      "find": "  pollOptions(poll ?? {})\n", "replace": "\n" },
    { "id": "execute-poll-options-validate", "file": "src/execute.mjs", "guard": "invalid poll options fail before anything is dispatched",
      "find": "if (typeof v !== 'number' || !Number.isFinite(v) || v < 0 || v > MAX_TIMER_MS || (!allowZero && v === 0)) throw", "replace": "if (false) throw" },
    { "id": "execute-poll-maxwait", "file": "src/execute.mjs", "guard": "the poll wait ends at maxWaitMs, not later",
      "find": "if (now() - start >= maxWait) throw", "replace": "if (now() - start > maxWait * 4) throw" },
    { "id": "execute-unknown-status-first-reply", "file": "src/execute.mjs", "guard": "an unrecognised status stops the render instead of polling to a timeout",
      "find": "if (status !== undefined && !IN_PROGRESS.has(status) && !DONE.has(status)) {", "replace": "if (FAILED.has(status)) {" },

    { "id": "dkg-seal-author", "file": "src/dkg.mjs", "guard": "a sealed asset must be authored by the expected address",
      "find": "if (expectAuthor && authorAddress !== String(expectAuthor).toLowerCase()) {\n      throw new DkgWriteError(`asset ${name} was sealed by", "replace": "if (false) {\n      throw new DkgWriteError(`asset ${name} was sealed by" },
    { "id": "dkg-resume-author", "file": "src/dkg.mjs", "guard": "resume refuses an asset recorded under another author",
      "find": "if (expectAuthor && authorAddress !== String(expectAuthor).toLowerCase()) {\n      refuse(", "replace": "if (false) {\n      refuse(" },
    { "id": "dkg-existing-name", "file": "src/dkg.mjs", "guard": "an existing asset name is refused without resume",
      "find": "if (existing && !resume) {", "replace": "if (false) {" },
    { "id": "dkg-resume-unknown-status", "file": "src/dkg.mjs", "guard": "resume refuses an asset in an unknown state",
      "find": "refuse(`the node reports status ${d.status ?? 'unknown'}`)", "replace": "return { name, ual: d.publishedUal ?? null, txHash: null, resumed: true }" },
    { "id": "dkg-tentative-ual", "file": "src/dkg.mjs", "guard": "a tentative UAL is not proof of a publish",
      "find": "const CONFIRMED_UAL = /^did:dkg:[a-z0-9]+:\\d+\\/(0x[0-9a-fA-F]{40})\\/(\\d+)$/", "replace": "const CONFIRMED_UAL = /^did:dkg:[a-z0-9]+:\\d+\\/(0x[0-9a-fA-F]{40})\\/t?([0-9a-f]+)$/" },
    { "id": "dkg-anchor-publisher", "file": "src/dkg.mjs", "guard": "a reconciled UAL must name the sealing author",
      "find": "if (author && publisher !== String(author).toLowerCase()) throw", "replace": "if (false) throw" },
    { "id": "dkg-anchor-merkle-root", "file": "src/dkg.mjs", "guard": "the published assertion must be the one sealed",
      "find": "if (merkleRoot && vm && vm !== bareHex(merkleRoot)) throw", "replace": "if (false) throw" },
    { "id": "dkg-reconcile-descriptor-status", "file": "src/dkg.mjs", "guard": "a lost publish reconciles only a vm-confirmed descriptor",
      "find": "if (d?.status !== 'vm-confirmed') throw new Error(", "replace": "if (false) throw new Error(" },
    { "id": "dkg-response-cap", "file": "src/dkg.mjs", "guard": "a response body over the limit is not read",
      "find": "if (bytes > limit) {", "replace": "if (false) {" },
    { "id": "dkg-publish-5xx-possibly-sent", "file": "src/dkg.mjs", "guard": "a publish answered 500 or 502 may have sent a transaction...",
      "find": "e.status >= 400 && e.status < 500", "replace": "e.status >= 400 && e.status < 600" },
    { "id": "dkg-publish-500-boundary", "file": "src/dkg.mjs", "guard": "a publish answered 500 or 502 may have sent a transaction...",
      "find": "e.status >= 400 && e.status < 500", "replace": "e.status >= 400 && e.status <= 500" },
    { "id": "dkg-publish-status0-not-prechain", "file": "src/dkg.mjs", "guard": "REAL: a lost response to a confirmed publish is reconciled...",
      "find": "e.status >= 400 && e.status < 500", "replace": "e.status < 500" },
    { "id": "dkg-publish-4xx-not-reconciled", "file": "src/dkg.mjs", "guard": "a publish refused with 4xx sent nothing: stage publish, mayHaveSent false, no reconcile",
      "find": "const beforeChain = e instanceof DkgHttpError && e.status >= 400 && e.status < 500", "replace": "const beforeChain = false" },
    { "id": "dkg-publish-200-unconfirmed-reconciled", "file": "src/dkg.mjs", "guard": "a 200 publish that is not confirmed, or cannot be parsed, is reconciled with mayHaveSent true",
      "find": "      return this.#reconcileLost(name, contextGraphId, sealed, e)\n    }\n    return {", "replace": "      throw new DkgWriteError(e.message, { name, stage: 'publish', status: published.status, body: b })\n    }\n    return {" },
    { "id": "dkg-publish-200-needs-ual", "file": "src/dkg.mjs", "guard": "a 200 publish that is not confirmed, or cannot be parsed... (confirmed without a UAL)",
      "find": "if (b.status !== 'confirmed' || !b.ual) {", "replace": "if (b.status !== 'confirmed') {" },
    { "id": "dkg-unbound-may-have-sent", "file": "src/dkg.mjs", "guard": "a 207 publish is a hard failure that still reports what was minted",
      "find": "txHash: b.txHash ?? null, mayHaveSent: true })", "replace": "txHash: b.txHash ?? null, mayHaveSent: false })" },
    { "id": "dkg-lost-carries-txhash", "file": "src/dkg.mjs", "guard": "a publish answered 500 or 502 may have sent a transaction... (502 carries txHash)",
      "find": "txHash: reported(body.txHash), mayHaveSent: true })", "replace": "txHash: null, mayHaveSent: true })" },
    { "id": "dkg-cap-nonstream-unknown-size", "file": "src/dkg.mjs", "guard": "a response without a readable stream is refused unless a declared length bounds it, and never buffered first",
      "find": "    if (!Number.isFinite(declared)) return null\n", "replace": "\n" },
    { "id": "dkg-cap-nonstream-lying-length", "file": "src/dkg.mjs", "guard": "a response without a readable stream is refused unless...",
      "find": "return Buffer.byteLength(text) > limit ? null : text", "replace": "return text" },
    { "id": "dkg-cap-missing-length-not-zero", "file": "src/dkg.mjs", "guard": "a response without a readable stream is refused unless...",
      "find": "typeof raw === 'string' && /^\\s*\\d+\\s*$/.test(raw) ? Number(raw) : NaN", "replace": "Number(raw)" },
    { "id": "dkg-cap-null-body-status", "file": "src/dkg.mjs", "guard": "a response without a readable stream is refused unless... (204)",
      "find": "    if (!res.body && NULL_BODY_STATUSES.has(res.status)) return ''\n", "replace": "\n" },
    { "id": "dkg-anchor-subject-filter", "file": "src/dkg.mjs", "guard": "a lost publish is not success unless the _meta anchor passes every resolver rule",
      "find": "const own = rows.filter(r => asIri(r.s) === ual)", "replace": "const own = rows" },
    { "id": "dkg-anchor-resolver-rules", "file": "src/dkg.mjs", "guard": "a lost publish is not success unless the _meta anchor passes every resolver rule",
      "find": "const anchor = anchors.get(expectedGraph)", "replace": "const anchor = anchors.get(expectedGraph) ?? problems.find(p => p.ual === ual)" },
    { "id": "dkg-anchor-merkle-root-one", "file": "src/dkg.mjs", "guard": "a lost publish is not success unless the _meta anchor passes every resolver rule (two merkle roots)",
      "find": "(roots.length !== 1 || roots[0] !== bareHex(merkleRoot))", "replace": "(roots[0] !== bareHex(merkleRoot))" },
    { "id": "dkg-anchor-merkle-root-equal", "file": "src/dkg.mjs", "guard": "a lost publish is not success unless the _meta anchor passes every resolver rule (another merkle root)",
      "find": "(roots.length !== 1 || roots[0] !== bareHex(merkleRoot))", "replace": "(roots.length !== 1)" },
    { "id": "dkg-resume-sealed-with-published", "file": "src/dkg.mjs", "guard": "resume refuses a sealed or shared record that also names a published assertion, without publishing",
      "find": "if (d.publishedUal || d.vmCurrentAssertion) refuse(", "replace": "if (false) refuse(" },
    { "id": "dkg-resume-sealed-with-published-ual", "file": "src/dkg.mjs", "guard": "resume refuses a sealed or shared record that also names a published assertion...",
      "find": "if (d.publishedUal || d.vmCurrentAssertion) refuse(", "replace": "if (d.vmCurrentAssertion) refuse(" },
    { "id": "dkg-resume-sealed-with-vm-pointer", "file": "src/dkg.mjs", "guard": "resume refuses a sealed or shared record that also names a published assertion...",
      "find": "if (d.publishedUal || d.vmCurrentAssertion) refuse(", "replace": "if (d.publishedUal) refuse(" },
    { "id": "dkg-resume-author-anchor", "file": "src/dkg.mjs", "guard": "resume refuses a published record whose UAL names another publisher, even with a valid anchor for it",
      "find": "author: expectAuthor ?? authorAddress })", "replace": "author: null })" },
    { "id": "dkg-resume-compares-sealed-root", "file": "src/dkg.mjs", "guard": "resume refuses a tentative, unanchored, foreign or unknown asset and never publishes (_meta records another merkle root)",
      "find": "merkleRoot: root ? `0x${root}` : null, descriptor: d", "replace": "merkleRoot: null, descriptor: d" },
    { "id": "dkg-resume-root-from-working-copy", "file": "src/dkg.mjs", "guard": "resume refuses a tentative, unanchored, foreign or unknown asset... (_meta records another merkle root than the working copy)",
      "find": "const root = vm || wm", "replace": "const root = vm" },
    { "id": "dkg-resume-unreadable-retryable", "file": "src/dkg.mjs", "guard": "a _meta read that fails while resuming a published asset is retryable (resume-unverified), not a permanent refusal",
      "find": "        if (e instanceof AnchorUnreadableError) unverified(e.message)\n", "replace": "\n" },
    { "id": "dkg-resume-unreadable-typed", "file": "src/dkg.mjs", "guard": "a _meta read that fails while resuming a published asset is retryable...",
      "find": "throw new AnchorUnreadableError(`its anchor could not be read", "replace": "throw new Error(`its anchor could not be read" },
    { "id": "dkg-resume-unknown-publish-not-republished", "file": "src/dkg.mjs", "guard": "resume after a publish of unknown outcome never publishes a shared asset again",
      "find": "if (d.status === 'swm-shared' && lastPublishUnknown) {", "replace": "if (false) {" },
    { "id": "dkg-resume-unknown-publish-may-have-sent", "file": "src/dkg.mjs", "guard": "resume after a publish of unknown outcome never publishes a shared asset again",
      "find": "{ mayHaveSent: true })\n      }", "replace": "{ mayHaveSent: false })\n      }" },
    { "id": "dkg-last-publish-unknown-boolean", "file": "src/dkg.mjs", "guard": "resume after a publish of unknown outcome never publishes a shared asset again ('yes' refused)",
      "find": "if (typeof lastPublishUnknown !== 'boolean') throw", "replace": "if (false) throw" },

    { "id": "derivation-id-for-other-bytes", "file": "src/derivation.mjs", "guard": "a saved derivation id cannot be reused for other bytes",
      "find": "if (given && given.sha16 !== sha.slice(0, 16)) {", "replace": "if (false) {" },
    { "id": "derivation-reconcile-trusted", "file": "src/derivation.mjs", "guard": "only trusted edges account for billed renders",
      "find": "const trusted = derivations.filter(d => d?.trusted === true)", "replace": "const trusted = derivations" },
    { "id": "derivation-billed-plain-decimal", "file": "src/derivation.mjs", "guard": "billedUsd must be finite and non-negative, or null, and is checked before the media is downloaded",
      "find": "    decimalTerm(v, 'billedUsd')\n", "replace": "\n" },
    { "id": "derivation-reconcile-trusted-strict", "file": "src/derivation.mjs", "guard": "reconcile counts only trusted derivation edges",
      "find": "d?.trusted === true", "replace": "d?.trusted" },
    { "id": "derivation-last-publish-unknown-passed", "file": "src/derivation.mjs", "guard": "lastPublishUnknown reaches the node and must be a boolean",
      "find": "resume, lastPublishUnknown })", "replace": "resume })" },
    { "id": "derivation-last-publish-unknown-boolean", "file": "src/derivation.mjs", "guard": "lastPublishUnknown reaches the node and must be a boolean",
      "find": "if (typeof lastPublishUnknown !== 'boolean') throw", "replace": "if (false) throw" },

    { "id": "fetch-bytes-shared-budget", "file": "src/fetch-bytes.mjs", "guard": "the byte cap is one budget across retries",
      "find": "if (budget.left < 0) throw new FetchBytesError(", "replace": "if (false) throw new FetchBytesError(" },
    { "id": "fetch-bytes-undefined-cap", "file": "src/fetch-bytes.mjs", "guard": "an unusable maxBytes never switches the cap off",
      "find": "if (typeof v !== 'number' || !Number.isFinite(v) || v < min || (integer && !Number.isInteger(v))) {", "replace": "if (false) {" },
    { "id": "fetch-bytes-string-chunks", "file": "src/fetch-bytes.mjs", "guard": "string chunks from a custom fetch are counted by their byte length",
      "find": "if (typeof chunk === 'string') return Buffer.from(chunk, 'utf8')", "replace": "if (typeof chunk === 'string') return { byteLength: undefined }" },
    { "id": "fetch-bytes-read-deadline", "file": "src/fetch-bytes.mjs", "guard": "the deadline holds even when a custom fetch ignores the abort signal",
      "find": "await beforeDeadline(reader.next(), deadline - now(), stop)", "replace": "await reader.next()" },
    { "id": "fetch-bytes-fetch-deadline", "file": "src/fetch-bytes.mjs", "guard": "the deadline holds even when a custom fetch ignores the abort signal",
      "find": "const res = await beforeDeadline(fetch(u, { redirect: 'follow', signal: AbortSignal.timeout(remainingMs) }), remainingMs)", "replace": "const res = await fetch(u, { redirect: 'follow', signal: AbortSignal.timeout(remainingMs) })" },
    { "id": "fetch-bytes-timeout-cap", "file": "src/fetch-bytes.mjs", "guard": "the deadline holds even when a custom fetch ignores the abort signal",
      "find": "if (timeoutMs > MAX_TIMER_MS) throw", "replace": "if (false) throw" },
    { "id": "fetch-bytes-attempts-validate", "file": "src/fetch-bytes.mjs", "guard": "the deadline holds even when a custom fetch ignores the abort signal",
      "find": "const attempts = option(opts, 'attempts', { integer: true, min: 1 })", "replace": "const attempts = opts.attempts ?? 4" },
    { "id": "fetch-bytes-deadline-per-attempt", "file": "src/fetch-bytes.mjs", "guard": "one deadline: a connection that drops late is retried only for what is left of it",
      "find": "      return await hashOnce(u, { maxBytes, budget, deadline, fetch, now })", "replace": "      return await hashOnce(u, { maxBytes, budget, deadline: now() + timeoutMs, fetch, now })" },
    { "id": "fetch-bytes-deadline-break", "file": "src/fetch-bytes.mjs", "guard": "one deadline: a connection that drops late is retried only for what is left of it",
      "find": "if (now() >= deadline) break", "replace": "" },
    { "id": "fetch-bytes-declared-vs-budget", "file": "src/fetch-bytes.mjs", "guard": "a retry whose declared size is more than the budget left is refused before reading",
      "find": "declared > Math.min(maxBytes, budget.left)", "replace": "declared > maxBytes" },
    { "id": "pending-protected-overwrite", "file": "src/pending.mjs", "guard": "a rerun cannot overwrite a submitted, rendered or recorded record",
      "find": "if (existing && !overwrite && (PROTECTED_STATUSES.includes(existing.status) || mayBeBilled(existing))) throw", "replace": "if (false) throw" },
    { "id": "pending-derivation-id-reuse", "file": "src/pending.mjs", "guard": "a derivation attempt refuses a changed id and invalid id, stage or mayHaveSent",
      "find": "if (prev.id && id !== prev.id) throw", "replace": "if (false) throw" },
    { "id": "pending-derivation-id-validate", "file": "src/pending.mjs", "guard": "a derivation attempt refuses a changed id and invalid id, stage or mayHaveSent",
      "find": "const id = pick('id', DERIVATION_ID, 'id')", "replace": "const id = next.id ?? prev.id ?? null" },
    { "id": "pending-derivation-stage-validate", "file": "src/pending.mjs", "guard": "a derivation attempt refuses a changed id and invalid id, stage or mayHaveSent",
      "find": "if (stage !== null && (typeof stage !== 'string' || !/^[a-z][a-z-]{0,39}$/.test(stage))) throw", "replace": "if (false) throw" },
    { "id": "pending-derivation-mhs-validate", "file": "src/pending.mjs", "guard": "a derivation attempt refuses a changed id and invalid id, stage or mayHaveSent",
      "find": "if (next.mayHaveSent !== undefined && typeof next.mayHaveSent !== 'boolean') throw", "replace": "if (false) throw" },
    { "id": "pending-key-immutable-save", "file": "src/pending.mjs", "guard": "D3: a possibly-billed render keeps its key, its attempts and its place in pending spend",
      "find": "if (existing.idempotencyKey && next.idempotencyKey !== undefined && next.idempotencyKey !== existing.idempotencyKey) {", "replace": "if (false) {" },
    { "id": "pending-key-immutable-resume", "file": "src/pending.mjs", "guard": "D3: a possibly-billed render keeps its key, its attempts and its place in pending spend",
      "find": "if (record.idempotencyKey !== undefined && existing.idempotencyKey && record.idempotencyKey !== existing.idempotencyKey) {", "replace": "if (false) {" },
    { "id": "pending-no-downgrade-save", "file": "src/pending.mjs", "guard": "D3: a possibly-billed render keeps its key, its attempts and its place in pending spend",
      "find": "            next = { ...next, status: 'submitted', lastOutcome: 'failed' }", "replace": "            next = { ...next }" },
    { "id": "pending-no-downgrade-finish", "file": "src/pending.mjs", "guard": "D3: a possibly-billed render keeps its key, its attempts and its place in pending spend",
      "find": "if (next.status === 'failed') next = { ...next, status: 'submitted', lastOutcome: 'failed' }", "replace": "if (false) next = next" },
    { "id": "pending-attempts-kept", "file": "src/pending.mjs", "guard": "D3: a possibly-billed render keeps its key, its attempts and its place in pending spend",
      "find": "} else if (!Array.isArray(next.attempts) || next.attempts.length < before.length) {", "replace": "} else if (false) {" },
    { "id": "pending-clean-failure-not-billed", "file": "src/pending.mjs", "guard": "a clean failure is not billed, so a rerun may start afresh with a new key",
      "find": "return rec.attempts.some(a => a?.sentAt && a.mayHaveStarted !== false)", "replace": "return rec.attempts.length > 0 || true" },
    { "id": "pending-dispatching-in-flight", "file": "src/pending.mjs", "guard": "a dispatching record is protected: a crashed one is resumed, a live one is refused, a legacy one is not overwritten",
      "find": "if (existing.status === 'dispatching' && last && !last.endedAt && last.pid !== undefined && last.pid !== process.pid && isAlive(last.pid)) {", "replace": "if (false) {" },
    { "id": "pending-create-protect", "file": "src/pending.mjs", "guard": "a dispatching record is protected: a crashed one is resumed, a live one is refused, a legacy one is not overwritten",
      "find": "if (existing && !overwrite && (PROTECTED_STATUSES.includes(existing.status) || mayBeBilled(existing))) throw", "replace": "if (existing && !overwrite && PROTECTED_STATUSES.includes(existing.status)) throw" },
    { "id": "pending-legacy-dispatching-billed", "file": "src/pending.mjs", "guard": "a dispatching record is protected: a crashed one is resumed, a live one is refused, a legacy one is not overwritten",
      "find": "  if (!Array.isArray(rec.attempts)) return true\n", "replace": "\n" },
    { "id": "pending-lock-exclusive", "file": "src/pending.mjs", "guard": "a held lock is waited on and then refused; a stale one is cleared",
      "find": "const fd = openSync(lock, 'wx', 0o600)", "replace": "const fd = openSync(lock, 'w', 0o600)" },
    { "id": "pending-lock-live-holder", "file": "src/pending.mjs", "guard": "a held lock is waited on and then refused; a stale one is cleared",
      "find": "if (stale) rmSync(lock, { force: true })", "replace": "if (true) rmSync(lock, { force: true })" },
    { "id": "pending-read-error", "file": "src/pending.mjs", "guard": "an unreadable pending record is a PendingReadError naming the file",
      "find": "throw new PendingReadError(path, e)", "replace": "throw e" },

    { "id": "resolve-merged-view-missing", "file": "src/resolve.mjs", "guard": "on a node that holds a merged view, a view left out of every answer fails the read",
      "find": "const viewMissing = [...materialised].filter(", "replace": "const viewMissing = [].filter(" },
    { "id": "resolve-merged-view-probe-only-when-held", "file": "src/resolve.mjs", "guard": "a node that holds no merged view is not required to show one",
      "find": "const viewMissing = [...materialised].filter(", "replace": "const viewMissing = [...expectView].filter(" },
    { "id": "resolve-merged-view-probe-unanswered", "file": "src/resolve.mjs", "guard": "a merged-view probe that never answers fails the read",
      "find": "for (const cg of viewUnsettled.filter(c => probeAnswers.get(c) !== attempts)) {", "replace": "for (const cg of []) {" },
    { "id": "resolve-view-unsettled", "file": "src/resolve.mjs", "guard": "D6: a probe and merged view both left out on the first attempt only do not settle \"no merged view\"",
      "find": " && viewMissing.length === 0 && viewUnsettled.length === 0) break", "replace": " && viewMissing.length === 0 && viewUnsettled.every(cg => probeAnswers.has(cg))) break" },
    { "id": "resolve-view-every-attempt", "file": "src/resolve.mjs", "guard": "D6: on a node with no merged view, \"no view\" is believed only after the probe answers empty on every attempt",
      "find": "viewUnsettled.filter(c => probeAnswers.get(c) !== attempts)", "replace": "viewUnsettled.filter(c => !probeAnswers.has(c))" },
    { "id": "resolve-merged-view-any-nonactive", "file": "src/resolve.mjs", "guard": "a merged-view-only revocation whose \"active\" row comes first is still a revocation",
      "find": "} else if (inGrants && nonActive.has(key) && !vmIds.has(row.s)) {", "replace": "} else if (inGrants && row.value !== 'active' && !vmIds.has(row.s)) {" },
    { "id": "resolve-swm-any-nonactive", "file": "src/resolve.mjs", "guard": "a shared-memory state whose \"active\" row comes first still warns of the unanchored revocation",
      "find": "          if (nonActive.has(key)) {", "replace": "          if (row.value !== 'active') {" },
    { "id": "resolve-held-error-fails", "file": "src/resolve.mjs", "guard": "R19: when listing subscriptions gives a transport error / a 500 / a 404 ...",
      "find": "    else failures.push(unconfirmed(why))", "replace": "    else warnings.push(unconfirmed(why))" },
    { "id": "resolve-held-shape-fails", "file": "src/resolve.mjs", "guard": "R19: when listing subscriptions gives an unexpected answer ...",
      "find": "    failures.push(unconfirmed('unexpected subscriptions answer'))", "replace": "    warnings.push(unconfirmed('unexpected subscriptions answer'))" },
    { "id": "resolve-held-403-warns", "file": "src/resolve.mjs", "guard": "R19: an empty graph the node is not subscribed to is inconsistent; a node that cannot list subscriptions warns",
      "find": "if (e instanceof DkgHttpError && e.status === 403) warnings.push", "replace": "if (false) warnings.push" },
    { "id": "resolve-held-no-call-warns", "file": "src/resolve.mjs", "guard": "R19: a node with no subscriptions call warns that an empty graph was not confirmed",
      "find": "    warnings.push(unconfirmed('the node has no subscriptions call'))\n", "replace": "" },
    { "id": "resolve-held-skip-current", "file": "src/resolve.mjs", "guard": "R19: a graph the freshness check found current is not asked about again",
      "find": "const ids = contextGraphIds.filter(cg => !current.includes(cg))", "replace": "const ids = contextGraphIds" },
    { "id": "resolve-unresolved-excludes-read-grants", "file": "src/resolve.mjs", "guard": "D7: a grant read from a configured graph named under another address is not unresolved, and its revocation is judged",
      "find": " && !readGrantIds.has(id) })", "replace": " })" },
    { "id": "resolve-dedupe-derivations", "file": "src/resolve.mjs", "guard": "a node that answers a second spelling of a graph id with the same assets does not double spend",
      "find": "let derivations = dedupeByUal(derivationReads.flatMap(r => r.derivations))", "replace": "let derivations = derivationReads.flatMap(r => r.derivations)" },
    { "id": "resolve-discovered-trusted", "file": "src/resolve.mjs", "guard": "a misplaced state that only discovery sees is a trusted forgery under a trusted producer, untrusted under a stranger",
      "find": "            trusted: !inGrants && trustedProducers.includes(path.publisher),\n            claims: { stateOf", "replace": "            trusted: false,\n            claims: { stateOf" },

    { "id": "livepeer-expired-typographic", "file": "src/livepeer.mjs", "guard": "typographic apostrophes and hyphens do not read as expired",
      "find": "export function saysExpired(text) {\n  const t = plainPunctuation(text)", "replace": "export function saysExpired(text) {\n  const t = String(text ?? '')" },
    { "id": "livepeer-text-url-host", "file": "src/livepeer.mjs", "guard": "a text-derived clip URL is agent.livepeer.org/a/ only",
      "find": "if (!u || u.protocol !== 'https:' || u.hostname !== 'agent.livepeer.org' || !u.pathname.startsWith('/a/')) continue", "replace": "if (!u) continue" },
    { "id": "livepeer-text-url-not-waiting", "file": "src/livepeer.mjs", "guard": "no text URL while the text says the upload is still to come",
      "find": " && !saysWaiting(text) &&", "replace": " &&" },
    { "id": "livepeer-text-url-received", "file": "src/livepeer.mjs", "guard": "with no status, the text must say the upload arrived",
      "find": " && (status !== null || saysReceived(text))", "replace": "" },

    { "id": "cli-consent-script-only", "file": "bin/mandate.mjs", "guard": "only a reading of the consent script is accepted without a person",
      "find": "const scriptMatched = r.scope.confirmed === true && sm?.matched === true", "replace": "const scriptMatched = r.scope.affirmative === true", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-consent-nonmatch-questions", "file": "bin/mandate.mjs", "guard": "a transcript that is not the script is put to the transcript and meaning questions",
      "find": "const questions = scriptMatched ? [] : [", "replace": "const questions = true ? [] : [", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-consent-typed-answer", "file": "bin/mandate.mjs", "guard": "each typed consent answer must be exactly the expected word (M12)",
      "find": "    if (got !== q.expect) return false\n", "replace": "", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-consent-json-impossible", "file": "bin/mandate.mjs", "guard": "a needed typed consent confirmation is impossible in --json mode: exit 3",
      "find": "if (questions.length && (!prompter.possible() || out.json)) {", "replace": "if (questions.length && !prompter.possible()) {", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-consent-script-covers-said-only", "file": "bin/mandate.mjs", "guard": "a script match still asks about terms the script never says",
      "find": "const items = scriptMatched ? unchecked.filter(i => !saidByScript(i)) : unchecked", "replace": "const items = scriptMatched ? [] : unchecked", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-consent-script-ceiling-said", "file": "bin/mandate.mjs", "guard": "a missing ceiling is never taken as said by the script",
      "find": "(item === 'ceiling' && grant.maxSpendUsd != null)", "replace": "(item === 'ceiling')", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-consent-clip-hash-script-only", "file": "bin/mandate.mjs", "guard": "only a script match is published with the clip hash (M10)",
      "find": "grant.consentClipSha256 = r.scriptMatched ? consent.sha256 : null", "replace": "grant.consentClipSha256 = consent.sha256", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-consent-no-tty-before-yes", "file": "bin/mandate.mjs", "guard": "grant --with-consent off a terminal is exit 3 with or without --yes, before capture",
      "find": "if (flags.withConsent && !prompter.possible()) {", "replace": "if (false) {", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-grant-confirm-possible", "file": "bin/mandate.mjs", "guard": "without --yes, --json or no terminal stops before capture (M3)",
      "find": "if (!flags.yes && (!prompter.possible() || out.json)) throw new UsageError('not on a terminal: pass --yes to publish without confirmation')", "replace": "", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-consent-affirmative", "file": "bin/mandate.mjs", "guard": "no first-person consent is never overridable (M14)",
      "find": "if (r.scope.affirmative !== true) {", "replace": "if (false) {", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-consent-unheard-needs-force", "file": "bin/mandate.mjs", "guard": "terms the heuristics did not hear need --force",
      "find": "if (!force) {", "replace": "if (false) {", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-render-reuse-billed-key", "file": "bin/mandate.mjs", "guard": "a rerun without --idempotency-key reuses a possibly-billed render's stored key",
      "find": "const idempotencyKey = flags.idempotencyKey ?? (mayBeBilled(pending.load(key)) ? undefined : key)", "replace": "const idempotencyKey = flags.idempotencyKey ?? key", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-render-begin-attempt", "file": "bin/mandate.mjs", "guard": "a rerun resumes a possibly-billed record instead of overwriting it",
      "find": "begun = pending.beginAttempt(record)", "replace": "begun = { record: pending.create(record, { overwrite: true }), resumed: false, attempt: { n: 1 } }", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-render-mark-sent", "file": "bin/mandate.mjs", "guard": "an attempt is marked sent before run_capability",
      "find": "      pending.markSent(key)\n", "replace": "", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-render-unknown-exit-9", "file": "bin/mandate.mjs", "guard": "an inline render whose outcome is unknown exits 9",
      "find": "return unknown ? EXIT.INCONCLUSIVE : e.kind === 'payment'", "replace": "return e.kind === 'payment'", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-at-execute", "file": "bin/mandate.mjs", "guard": "render --at is refused with --execute",
      "find": "if (flags.execute && flags.at) throw new UsageError(", "replace": "if (false) throw new UsageError(", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-local-pending-rendered", "file": "bin/mandate.mjs", "guard": "billed media not yet anchored counts against the ceiling (M6)",
      "find": "const OPEN_STATUSES = ['dispatching', 'submitted', 'rendered']", "replace": "const OPEN_STATUSES = ['dispatching', 'submitted']", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-local-pending-maybe-billed", "file": "bin/mandate.mjs", "guard": "a record that may be billed counts whatever its status",
      "find": "if (!(OPEN_STATUSES.includes(rec?.status) || mayBeBilled(rec)) || typeof rec.grantId !== 'string') continue", "replace": "if (!OPEN_STATUSES.includes(rec?.status) || typeof rec.grantId !== 'string') continue", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-local-pending-no-shrunk-estimate", "file": "bin/mandate.mjs", "guard": "local pending spend never trusts an estimate the operator can shrink",
      "find": "const billedUsd = finiteNonNegative(rec.costUsdEstimated) && finiteNonNegative(rec.estimateUsd) ? Math.max(rec.costUsdEstimated, rec.estimateUsd) : known", "replace": "const billedUsd = finiteNonNegative(rec.costUsdEstimated) ? rec.costUsdEstimated : finiteNonNegative(rec.estimateUsd) ? rec.estimateUsd : null", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-record-unknown-maybe-billed", "file": "bin/mandate.mjs", "guard": "record says unknown (exit 9) for a possibly-billed render with no job",
      "find": "const unknown = rec.status === 'dispatching' || mayBeBilled(rec)", "replace": "const unknown = rec.status === 'dispatching'", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-resumable-no-publish", "file": "bin/mandate.mjs", "guard": "a publish-stage attempt is not resumed without asking the node",
      "find": "const RESUMABLE_STAGES = new Set(['create', 'share', 'author'])", "replace": "const RESUMABLE_STAGES = new Set(['create', 'share', 'author', 'publish'])", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-last-publish-reported-tx", "file": "bin/mandate.mjs", "guard": "a reported UAL or transaction makes the last publish unknown",
      "find": "if (attempt.mayHaveSent === true || attempt.ual || attempt.txHash) return true", "replace": "if (attempt.mayHaveSent === true) return true", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-last-publish-4xx-resumable", "file": "bin/mandate.mjs", "guard": "a publish refused with 4xx sent nothing and may be retried",
      "find": "  if (attempt.stage === 'publish' && Number.isInteger(status) && status >= 400 && status < 500) return false\n", "replace": "", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-publish-status-saved", "file": "bin/mandate.mjs", "guard": "the HTTP status of a publish refusal is kept for the retry",
      "find": "const publishStatus = !blocked && stage === 'publish' && Number.isInteger(e?.status) ? e.status : null", "replace": "const publishStatus = null", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-resume-permanent-stages", "file": "bin/mandate.mjs", "guard": "an unbound or refused asset is never continued (M1)",
      "find": "if (PERMANENT_STAGES.has(attempt.stage)) {", "replace": "if (false) {", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-resume-null-after-send", "file": "bin/mandate.mjs", "guard": "a possibly-sent attempt whose asset the node does not show is not recreated (M2)",
      "find": "if (d === null && attempt.mayHaveSent !== true) return null", "replace": "if (d === null) return null", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-write-failed-ids", "file": "bin/mandate.mjs", "guard": "a failed grant or revocation write reports its ids and asset (D5)",
      "find": "stage, ...ids, assetName, contextGraphId,", "replace": "stage, contextGraphId,", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-write-failed-check", "file": "bin/mandate.mjs", "guard": "an unknown write outcome says how to check it (D5)",
      "find": "check: unknown ? check : null", "replace": "check: null", "tests": ["test/cli.test.mjs"] },
    { "id": "cli-exit-pending-read", "file": "bin/mandate.mjs", "guard": "an unreadable pending record exits 9, not 1",
      "find": "|| e instanceof PendingReadError) return EXIT.INCONCLUSIVE", "replace": ") return EXIT.INCONCLUSIVE", "tests": ["test/cli.test.mjs"] },
    { "id": "nodes-freshness-integers", "file": "scripts/nodes.mjs", "guard": "a reconcile reply without whole head and watermark is never current",
      "find": "  if (!ok(r?.watermarkAfter) || !ok(r?.headOrdinal)) {", "replace": "  if (false) {", "tests": ["test/cli.test.mjs"] },
    { "id": "nodes-doctor-not-current", "file": "scripts/nodes.mjs", "guard": "doctor exits 9 unless every graph is current",
      "find": "if (!s?.subscribed || !f.current) process.exitCode = 9", "replace": "if (!s?.subscribed || f.unknown === undefined) process.exitCode = 9", "tests": ["test/cli.test.mjs"] },
    { "id": "nodes-up-early-exit", "file": "scripts/nodes.mjs", "guard": "up stops before subscribe while graph ids are placeholders (R16)",
      "find": "    process.exit(0)\n  }\n  if (command === 'up' || command === 'subscribe')", "replace": "  }\n  if (command === 'up' || command === 'subscribe')", "tests": ["test/cli.test.mjs"] },
    { "id": "demo-blast-exit", "file": "demo/full.mjs", "guard": "the demo fails on a blast radius that did not exit 0",
      "find": "  if (blast.exitCode !== 0) fail('blast radius', blast)\n", "replace": "", "tests": ["test/cli.test.mjs"] },
    { "id": "demo-blast-unreadable", "file": "demo/full.mjs", "guard": "the demo names unreadable trusted records",
      "find": "const unreadable = r.unreadable ?", "replace": "const unreadable = false ?", "tests": ["test/cli.test.mjs"] },
    { "id": "demo-blast-label", "file": "demo/full.mjs", "guard": "the demo uses the CLI's billed label, not list price",
      "find": "billed (as recorded by producers; not an invoice)", "replace": "billed at list price", "tests": ["test/cli.test.mjs"] },
    { "id": "s6c-fixed-decision-time", "file": "test/integration/s6c.test.mjs", "guard": "the integration test decides inside G2's validity window",
      "find": "at: DECIDE_AT,", "replace": "at: new Date().toISOString(),", "tests": ["test/cli.test.mjs"] },

    { "id": "mutation-check-timeout-not-kill", "file": "scripts/mutation-check.mjs", "guard": "a run cut off by the timeout is not a kill",
      "find": "if (r.timedOut) return { status: 'timeout', detail: `the tests did not finish within ${timeoutMs}ms` }", "replace": "if (r.timedOut) return { status: 'killed', detail: `the tests did not finish within ${timeoutMs}ms` }", "tests": ["test/release.test.mjs"] },
    { "id": "mutation-check-cancelled-not-kill", "file": "scripts/mutation-check.mjs", "guard": "a run whose only non-passing tests were cancelled is not a kill",
      "find": "if (fail === 0 && cancelled > 0) return", "replace": "if (false) return", "tests": ["test/release.test.mjs"] },
    { "id": "mutation-check-timeout-fails-run", "file": "scripts/mutation-check.mjs", "guard": "a timed-out mutant fails the check",
      "find": "return survived || timedOut || stale || invalid ? 1 : 0", "replace": "return survived || stale || invalid ? 1 : 0", "tests": ["test/release.test.mjs"] },
    { "id": "release-pack-before-install", "file": ".github/workflows/release.yml", "guard": "the release tarball is packed before any dependency code runs",
      "find": "npm pack --ignore-scripts --pack-destination dist", "replace": "npm ci && npm pack --pack-destination dist", "tests": ["test/release.test.mjs"] },
    { "id": "release-test-ignore-scripts", "file": ".github/workflows/release.yml", "guard": "the release test job installs with no install scripts",
      "find": "      - run: npm ci --ignore-scripts\n      - run: npm test", "replace": "      - run: npm ci\n      - run: npm test", "tests": ["test/release.test.mjs"] },
    { "id": "release-publish-needs-test", "file": ".github/workflows/release.yml", "guard": "publish waits for the test job",
      "find": "needs: [pack, test]", "replace": "needs: [pack]", "tests": ["test/release.test.mjs"] },
    { "id": "ci-docs-untracked", "file": ".github/workflows/ci.yml", "guard": "the namespace documents check sees untracked files",
      "find": "changes=\"$(git status --porcelain --untracked-files=all -- docs/ns)\"", "replace": "changes=\"$(git diff --name-only -- docs/ns)\"", "tests": ["test/release.test.mjs"] },
    { "id": "ci-install-ignore-scripts", "file": ".github/workflows/ci.yml", "guard": "CI installs with no install scripts",
      "find": "      - run: npm ci --ignore-scripts\n      - name: Unit", "replace": "      - run: npm ci\n      - name: Unit", "tests": ["test/release.test.mjs"] },

    { "id": "dkg-anchor-asks-merkle-root", "file": "src/dkg.mjs", "guard": "the lost-publish anchor read asks _meta for the minted merkle root",
      "find": "{ limit: 50, merkleRoot: true }", "replace": "{ limit: 50 }" },
    { "id": "queries-meta-merkle-root", "file": "src/queries.mjs", "guard": "the writer anchor query names dkg:merkleRoot",
      "find": ", <${DKG}merkleRoot>` : metaPredicates()", "replace": "` : metaPredicates()" }
  ]
}
