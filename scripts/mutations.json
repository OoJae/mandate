{
  "about": "Security guards for scripts/mutation-check.mjs. Each entry removes or weakens one guard; the test suite must fail for every one. When a refactor moves a guard, update its find/replace here rather than deleting the entry.",
  "slowTests": ["test/cli.test.mjs"],
  "mutants": [
    { "id": "gate-grantor-did", "file": "src/gate.mjs", "guard": "a grant's grantor DID must name its publisher",
      "find": " && agentAddress(g.grantor) === a", "replace": "" },
    { "id": "gate-grant-iri-address", "file": "src/gate.mjs", "guard": "a grant id must embed its publisher's address",
      "find": " && grantIriAddress(g.id) === a", "replace": "" },
    { "id": "gate-subject-address", "file": "src/gate.mjs", "guard": "a grant's subject must belong to its publisher",
      "find": "subjectAddress(g.subject) === a && ", "replace": "" },
    { "id": "gate-vm-tier-only", "file": "src/gate.mjs", "guard": "only Verifiable Memory grants are authentic",
      "find": "if (!g || g.tier !== 'vm') return false", "replace": "if (!g) return false" },
    { "id": "gate-active-state-counted", "file": "src/gate.mjs", "guard": "an active state is not a revocation",
      "find": "s.state !== 'active' && (", "replace": "(" },
    { "id": "gate-context-tier-revocation", "file": "src/gate.mjs", "guard": "a merged-view revocation with no publisher is honoured",
      "find": "\n    || s.tier === 'context'))", "replace": "))" },
    { "id": "gate-revocation-publisher", "file": "src/gate.mjs", "guard": "a vm revocation counts only from the grant's publisher",
      "find": "s.publisher.toLowerCase() === grant.publisher.toLowerCase()", "replace": "true" },
    { "id": "gate-untrusted-spend", "file": "src/gate.mjs", "guard": "prior spend comes only from trusted producers",
      "find": "filter(d => d?.trusted === true && d.authorizedUnder === grantId)", "replace": "filter(d => d?.authorizedUnder === grantId)" },
    { "id": "gate-unreadable-trusted-spend", "file": "src/gate.mjs", "guard": "an unreadable trusted record under the grant makes spend unknown",
      "find": "const unknown = unreadable > 0 || under.some(", "replace": "const unknown = under.some(" },
    { "id": "gate-missing-billed-spend", "file": "src/gate.mjs", "guard": "a trusted edge with no billed amount makes spend unknown",
      "find": "const unknown = unreadable > 0 || under.some(d => !finiteNonNegative(d.billedUsd))", "replace": "const unknown = unreadable > 0" },
    { "id": "gate-valid-from", "file": "src/gate.mjs", "guard": "a grant is not used before validFrom",
      "find": "if (when < from) {", "replace": "if (false) {" },
    { "id": "gate-valid-until", "file": "src/gate.mjs", "guard": "a grant is not used after validUntil",
      "find": "if (when > until) {", "replace": "if (false) {" },
    { "id": "gate-unreadable-window", "file": "src/gate.mjs", "guard": "an unreadable validity window refuses",
      "find": "if (Number.isNaN(from) || Number.isNaN(until)) {\n      fail(4", "replace": "if (false) {\n      fail(4" },
    { "id": "gate-ignore-prior-spend", "file": "src/gate.mjs", "guard": "the ceiling includes spend already committed",
      "find": "prior.micro + estimateMicro > microUsd(ceiling, { down: true })", "replace": "estimateMicro > microUsd(ceiling, { down: true })" },
    { "id": "gate-ceiling-boundary", "file": "src/gate.mjs", "guard": "a render exactly at the ceiling is permitted, one over is not",
      "find": "prior.micro + estimateMicro > microUsd(ceiling, { down: true })", "replace": "prior.micro + estimateMicro >= microUsd(ceiling, { down: true })" },
    { "id": "gate-unknown-spend", "file": "src/gate.mjs", "guard": "an unknown spend under a ceiling refuses",
      "find": "if (spend.unknown) {", "replace": "if (false) {" },
    { "id": "gate-unreadable-ceiling", "file": "src/gate.mjs", "guard": "an unreadable ceiling refuses",
      "find": "if (!finiteNonNegative(ceiling)) { fail(6", "replace": "if (false) { fail(6" },
    { "id": "gate-micro-round-up", "file": "src/gate.mjs", "guard": "sub-micro-dollar amounts round up, so tiny renders cannot slip under a ceiling",
      "find": "if (!down && diff > tol) return m + 1n", "replace": "" },
    { "id": "gate-missing-consistency", "file": "src/gate.mjs", "guard": "knowledge without a consistent read refuses",
      "find": "if (k.consistency?.ok !== true)", "replace": "if (k.consistency?.ok === false)" },
    { "id": "gate-duplicate-grant-id", "file": "src/gate.mjs", "guard": "a grant id published twice is refused",
      "find": "if (copies.get(g.id) > 1) {", "replace": "if (false) {" },
    { "id": "gate-clause-list-strings", "file": "src/gate.mjs", "guard": "clause lists must be arrays, never substring-matched strings",
      "find": "Array.isArray(v) ? v : null)", "replace": "v)" },
    { "id": "gate-capability", "file": "src/gate.mjs", "guard": "the capability must be permitted",
      "find": "if (!Array.isArray(g.permitsCapability) || !g.permitsCapability.includes(capability)) {", "replace": "if (false) {" },
    { "id": "gate-forbidden-use-class", "file": "src/gate.mjs", "guard": "a forbidden use class refuses",
      "find": "if (forbids.includes(useClass)) {", "replace": "if (false) {" },
    { "id": "gate-territory", "file": "src/gate.mjs", "guard": "the territory must be covered",
      "find": "if (territories.length && !territories.includes(territory)) {", "replace": "if (false) {" },
    { "id": "gate-prohibited-use-class", "file": "src/gate.mjs", "guard": "prohibited use classes refuse whatever a grant says",
      "find": "if (isProhibitedUseClass(request.useClass)) {", "replace": "if (false) {" },
    { "id": "policy-prohibited-words", "file": "src/policy.mjs", "guard": "a label is refused when any of its words is prohibited",
      "find": "return label.split('-').some(word => PROHIBITED.has(word))", "replace": "return false" },

    { "id": "prov-grant-id-mismatch", "file": "src/provenance.mjs", "guard": "a grant id naming another address is a forgery",
      "find": "if (grantIriAddress(g.id) !== g.publisher) {", "replace": "if (false) {" },
    { "id": "prov-grant-not-by-subject", "file": "src/provenance.mjs", "guard": "a grant for someone else's subject is a forgery",
      "find": "if (g.subjectAddress !== g.publisher) {", "replace": "if (false) {" },
    { "id": "prov-grantor-literal", "file": "src/provenance.mjs", "guard": "a grantor literal naming another address is a forgery",
      "find": "if (g.grantorAddress !== g.publisher) {", "replace": "if (false) {" },
    { "id": "prov-misplaced-grant", "file": "src/provenance.mjs", "guard": "a grant in a derivations graph is never accepted",
      "find": "if (role !== 'grants') { report('misplaced-grant'", "replace": "if (false) { report('misplaced-grant'" },
    { "id": "prov-state-not-by-grantor", "file": "src/provenance.mjs", "guard": "a state about another address's grant is a forgery",
      "find": "if (owner !== anchor.publisher) { report('state-not-by-grantor'", "replace": "if (false) { report('state-not-by-grantor'" },
    { "id": "prov-was-attributed-to", "file": "src/provenance.mjs", "guard": "wasAttributedTo must name the anchor's address",
      "find": "if (attributed.some(a => agentAddress(a) !== publisher)) {", "replace": "if (false) {" },
    { "id": "prov-unconfirmed-status", "file": "src/provenance.mjs", "guard": "only confirmed anchors count",
      "find": "if (statuses.length !== 1 || statuses[0] !== 'confirmed') {", "replace": "if (false) {" },
    { "id": "prov-assertion-graph", "file": "src/provenance.mjs", "guard": "an anchor's assertion graph must be the one its UAL derives",
      "find": "if (graphs.length !== 1 || graphs[0] !== expectedGraph) {", "replace": "if (false) {" },
    { "id": "prov-state-author", "file": "src/provenance.mjs", "guard": "a mismatched stateAuthor makes the state malformed (so revoked)",
      "find": "if (authorCell && agentAddress(stateAuthor) !== anchor.publisher) {", "replace": "if (false) {" },
    { "id": "prov-malformed-state-active", "file": "src/provenance.mjs", "guard": "the grantor's malformed state counts as revoked, never active",
      "find": "state: raw === 'active' && problems.length === 0 ? 'active' : 'revoked',", "replace": "state: raw === 'active' ? 'active' : 'revoked'," },
    { "id": "prov-unreadable-state-of", "file": "src/provenance.mjs", "guard": "a state with an unreadable stateOf makes the read inconsistent",
      "find": "if (stateOf === null) {", "replace": "if (false) {" },
    { "id": "prov-derivation-id-mismatch", "file": "src/provenance.mjs", "guard": "a derivation id must match its output hash",
      "find": "if (!d.id.startsWith(", "replace": "if (false && !d.id.startsWith(" },
    { "id": "prov-trusted-legacy-format", "file": "src/provenance.mjs", "guard": "a trusted producer's legacy-format record is a forgery, not a warning",
      "find": "if (isTrusted()) report('legacy-format'", "replace": "if (false) report('legacy-format'" },
    { "id": "prov-forgery-trusted-flag", "file": "src/provenance.mjs", "guard": "a forgery under a trusted producer's prefix is marked trusted",
      "find": "if (role === 'derivations') return trusted.includes(pathPublisher)", "replace": "if (role === 'derivations') return false" },
    { "id": "prov-derivation-trusted", "file": "src/provenance.mjs", "guard": "a derivation is trusted only under a trusted producer",
      "find": "trusted: trustedProducers.includes(anchor.publisher),", "replace": "trusted: true," },
    { "id": "prov-derived-at-required", "file": "src/provenance.mjs", "guard": "a derivation must carry derivedAt",
      "find": "derivedAt: single(obj, V.derivedAt, asIso),", "replace": "derivedAt: single(obj, V.derivedAt, asIso, { required: false }) ?? null," },
    { "id": "prov-invalid-cell-dropped", "file": "src/provenance.mjs", "guard": "an unparseable cell is kept as invalid, never silently dropped",
      "find": "const o = parseCell(row.o) ?? { type: 'invalid', value: '' }", "replace": "const o = parseCell(row.o) ?? { type: 'invalid', value: '' }\n    if (o.type === 'invalid') continue" },
    { "id": "prov-known-uals", "file": "src/provenance.mjs", "guard": "an anchor seen before must still be present",
      "find": "if (missing.length) return fail(", "replace": "if (false) return fail(" },
    { "id": "prov-triple-count", "file": "src/provenance.mjs", "guard": "each anchored graph returns its declared triple count",
      "find": "if (n !== a.publicTripleCount) return fail(", "replace": "if (false) return fail(" },
    { "id": "prov-graph-count", "file": "src/provenance.mjs", "guard": "the node's graph count must match the anchors",
      "find": "if (visibleGraphCount !== undefined\n", "replace": "if (false\n" },
    { "id": "prov-unanchored-graph", "file": "src/provenance.mjs", "guard": "a returned graph with no anchor makes the read inconsistent",
      "find": "if (!anchors.has(g) && !pendingGraphs.has(g)) return fail(", "replace": "if (false) return fail(" },

    { "id": "resolve-freshness-status-0", "file": "src/resolve.mjs", "guard": "an unreachable freshness check fails the read",
      "find": "if (e.status === 403) warnings.push(", "replace": "if (e.status === 403 || e.status === 0) warnings.push(" },
    { "id": "resolve-freshness-http-errors", "file": "src/resolve.mjs", "guard": "404/409/429/5xx from reconcile fail the read",
      "find": "if (e.status === 403) warnings.push(", "replace": "if (e.status !== 0) warnings.push(" },
    { "id": "resolve-stale-only-warns", "file": "src/resolve.mjs", "guard": "a stale node fails the read",
      "find": "failures.push(`stale view:", "replace": "warnings.push(`stale view:" },
    { "id": "resolve-watermark-ahead", "file": "src/resolve.mjs", "guard": "a watermark ahead of the head fails the read",
      "find": "} else if (have > head || r?.status === 'watermark-ahead') {", "replace": "} else if (false) {" },
    { "id": "resolve-freshness-malformed", "file": "src/resolve.mjs", "guard": "a head or watermark that is not a number fails the read",
      "find": "if (head === null || have === null) {", "replace": "if (false) {" },
    { "id": "resolve-no-reconcile", "file": "src/resolve.mjs", "guard": "a node that cannot check freshness fails when the check is asked for",
      "find": "failures.push(`freshness of ${cg} cannot be checked", "replace": "warnings.push(`freshness of ${cg} cannot be checked" },
    { "id": "resolve-state-missing-from-read", "file": "src/resolve.mjs", "guard": "a grantor state visible in discovery must be in the grantor read",
      "find": "if (!inRead) failures.push(", "replace": "if (false) failures.push(" },
    { "id": "resolve-missing-graph-count", "file": "src/resolve.mjs", "guard": "a read with no graph count is inconsistent",
      "find": "if (visible < 0 && consistency.ok) Object.assign(", "replace": "if (false) Object.assign(" },
    { "id": "resolve-truncated-discovery", "file": "src/resolve.mjs", "guard": "a truncated discovery query fails the read",
      "find": "else if (e instanceof ReadTruncatedError) failures.push(`a discovery query", "replace": "else if (e instanceof ReadTruncatedError) warnings.push(`a discovery query" },
    { "id": "resolve-revocation-memory", "file": "src/resolve.mjs", "guard": "a revocation seen before is never forgotten",
      "find": "if (r && !states.some(", "replace": "if (false && !states.some(" },
    { "id": "resolve-empty-after-errors", "file": "src/resolve.mjs", "guard": "an empty read is believed only when every attempt answered",
      "find": "if (failed && anchors.size === 0) {", "replace": "if (false) {" },
    { "id": "resolve-pending-anchor", "file": "src/resolve.mjs", "guard": "an unconfirmed anchor in a deciding prefix makes the read inconsistent",
      "find": "if (problems.length) fail(", "replace": "if (false) fail(" },
    { "id": "resolve-retry-pending-anchor", "file": "src/resolve.mjs", "guard": "a read with anchor problems is retried",
      "find": "if (consistency.ok && anchors.size > 0 && problems.length === 0) break", "replace": "if (consistency.ok && anchors.size > 0) break" },
    { "id": "resolve-unreadable-objects", "file": "src/resolve.mjs", "guard": "unreadable objects in a deciding prefix make the read inconsistent",
      "find": "if (slice.unreadable.length) fail(", "replace": "if (false) fail(" },
    { "id": "resolve-paging", "file": "src/resolve.mjs", "guard": "a prefix read continues past its first page",
      "find": "if (page.length <= max) return rows", "replace": "return rows" },
    { "id": "resolve-discovery-failures-warn", "file": "src/resolve.mjs", "guard": "state discovery failures fail the read",
      "find": "failures.push(...f)", "replace": "warnings.push(...f)" },
    { "id": "resolve-discovery-unanswered", "file": "src/resolve.mjs", "guard": "a discovery query that never answers fails the read",
      "find": "if (answeredOnce.size !== queries.length) failures.push(", "replace": "if (false) failures.push(" },
    { "id": "resolve-merged-view-omitted", "file": "src/resolve.mjs", "guard": "a merged view left out of every answer fails the read",
      "find": "for (const cg of viewMissing) failures.push(", "replace": "for (const cg of []) failures.push(" },
    { "id": "resolve-discovery-batches", "file": "src/resolve.mjs", "guard": "state discovery covers every grant id, not the first 50",
      "find": ".flatMap(q => chunks(grantIds).map(batch => ({ ...q, batch })))", "replace": ".map(q => ({ ...q, batch: grantIds.slice(0, 50) }))" },
    { "id": "resolve-merged-view-revocation", "file": "src/resolve.mjs", "guard": "a revocation only in the merged view is honoured",
      "find": "} else if (inGrants && row.value !== 'active' && !vmIds.has(row.s)) {", "replace": "} else if (false) {" },
    { "id": "resolve-grant-discovery-failure", "file": "src/resolve.mjs", "guard": "grant discovery that does not answer fails the read",
      "find": "if (!got) throw new DiscoveryFailed(`grant discovery", "replace": "if (!got) continue\n    if (false) throw new DiscoveryFailed(`grant discovery" },
    { "id": "resolve-not-subscribed", "file": "src/resolve.mjs", "guard": "a graph the node does not hold (or holds in another case) fails the read",
      "find": "failures.push(...held.failures)", "replace": "warnings.push(...held.failures)" },
    { "id": "resolve-unresolved-grants", "file": "src/resolve.mjs", "guard": "grants in graphs this reader does not read are reported unresolved",
      "find": ".filter(id => { const o = grantIriAddress(id); return Boolean(o) && !grantsOwners.has(o) })", "replace": ".filter(() => false)" },

    { "id": "verify-derived-before-from", "file": "src/verify-core.mjs", "guard": "a render recorded before validFrom is TAINTED",
      "find": " || derived < from", "replace": "" },
    { "id": "verify-derived-after-until", "file": "src/verify-core.mjs", "guard": "a render recorded after expiry is TAINTED / UNAUTHORISED",
      "find": "if (derived > until) {", "replace": "if (false) {" },
    { "id": "verify-expired", "file": "src/verify-core.mjs", "guard": "an expired grant is TAINTED / EXPIRED",
      "find": "if (nowMs > until) {", "replace": "if (false) {" },
    { "id": "verify-revoked", "file": "src/verify-core.mjs", "guard": "a revoked grant is TAINTED / REVOKED",
      "find": "if (rev.revoked) {", "replace": "if (false) {" },
    { "id": "verify-capability", "file": "src/verify-core.mjs", "guard": "a capability the grant never permitted is TAINTED",
      "find": "if (!grant.permitsCapability.includes(d.servedCapability)) {", "replace": "if (false) {" },
    { "id": "verify-duplicate-grant", "file": "src/verify-core.mjs", "guard": "a grant id published twice is TAINTED / MALFORMED",
      "find": "if (found.length > 1) return out(", "replace": "if (false) return out(" },
    { "id": "verify-unreadable-derived-at", "file": "src/verify-core.mjs", "guard": "a trusted edge with no readable render time is MALFORMED",
      "find": "if (Number.isNaN(derived)) {", "replace": "if (false) {" },
    { "id": "verify-unreadable-window", "file": "src/verify-core.mjs", "guard": "an unreadable validity window is MALFORMED",
      "find": "if (Number.isNaN(from) || Number.isNaN(until)) {", "replace": "if (false) {" },
    { "id": "verify-untrusted-edges", "file": "src/verify-core.mjs", "guard": "untrusted edges are shown, never believed",
      "find": "const trusted = edges.filter(d => d.trusted === true)", "replace": "const trusted = edges" },
    { "id": "verify-trusted-forgery", "file": "src/verify-core.mjs", "guard": "a trusted producer's unreadable record for these bytes is TAINTED",
      "find": "if (!isTrusted) continue", "replace": "continue" },
    { "id": "verify-unresolved-unknown", "file": "src/verify-core.mjs", "guard": "an edge citing a grant in an unread graph is UNKNOWN, not TAINTED",
      "find": "if (unresolved.has(d.authorizedUnder)) {", "replace": "if (false) {" },
    { "id": "verify-tainted-precedence", "file": "src/verify-core.mjs", "guard": "any TAINTED judgement taints the file",
      "find": "const verdict = tainted.length ? TAINTED : unknown.length ? UNKNOWN : CLEAR", "replace": "const verdict = unknown.length ? UNKNOWN : tainted.length ? TAINTED : CLEAR" },
    { "id": "verify-inconsistent", "file": "src/verify-core.mjs", "guard": "knowledge without a consistent read is INCONCLUSIVE",
      "find": "if (k?.consistency?.ok !== true) {", "replace": "if (k?.consistency?.ok === false) {" },
    { "id": "verify-missing-lists", "file": "src/verify-core.mjs", "guard": "hand-built knowledge missing a list is INCONCLUSIVE",
      "find": "if (!Array.isArray(k[f])) return { ...base, verdict: INCONCLUSIVE", "replace": "if (false) return { ...base, verdict: INCONCLUSIVE" },
    { "id": "verify-blast-unreadable", "file": "src/verify-core.mjs", "guard": "unreadable trusted records make the blast radius's billing unknown",
      "find": "const unknown = unreadable > 0 || assets.some(d => !readable(d))", "replace": "const unknown = assets.some(d => !readable(d))" },

    { "id": "rdf-term-calendar", "file": "src/rdf-term.mjs", "guard": "calendar-impossible dates are refused, not rolled forward",
      "find": "if (!days || day < 1 || day > days || hour > 23 || minute > 59 || second > 59 || offH > 14 || offM > 59) return NaN", "replace": "" },
    { "id": "rdf-term-decimal-round-up", "file": "src/rdf-term.mjs", "guard": "a positive amount is never written as 0",
      "find": "if (shortfall > Math.max(1e-12, n * 4 * Number.EPSILON)) {", "replace": "if (false) {" },

    { "id": "scope-negator-words", "file": "src/scope.mjs", "guard": "a negator anywhere in a clause contradicts its terms",
      "find": "return words(t).some(w => NEGATOR_WORDS.has(w) || REFUSAL_STEMS.some(s => w.startsWith(s))) || NEGATOR_PHRASES.some(p => has(t, p))", "replace": "return NEGATOR_PHRASES.some(p => has(t, p))" },
    { "id": "scope-negator-phrases", "file": "src/scope.mjs", "guard": "phrases such as 'other than' and 'off limits' negate",
      "find": " || NEGATOR_PHRASES.some(p => has(t, p))", "replace": "" },
    { "id": "scope-guard-phrase-scope", "file": "src/scope.mjs", "guard": "a guard phrase removes only its own words ('I don't mind no ads')",
      "find": "for (const g of NOT_NEGATING) t = t.split(` ${g} `).join(' ')", "replace": "for (const g of NOT_NEGATING) if (has(t, g)) return ''" },
    { "id": "scope-but-after-universal", "file": "src/scope.mjs", "guard": "'everything but X' excludes X",
      "find": "' $1$2 except '", "replace": "' $1$2 but '" },
    { "id": "scope-refusal-verbs", "file": "src/scope.mjs", "guard": "a refusal verb anywhere contradicts consent",
      "find": "if (hasRefusal(norm)) {", "replace": "if (false) {" },
    { "id": "scope-retractions", "file": "src/scope.mjs", "guard": "a retraction contradicts consent",
      "find": "if (RETRACTIONS.some(r => has(unguardedAll, r))) {", "replace": "if (false) {" },
    { "id": "scope-negated-consent", "file": "src/scope.mjs", "guard": "consent said in a negated clause is contradicted",
      "find": "if (cl.negated && cl.consentWord) {", "replace": "if (false) {" },
    { "id": "scope-bare-refusal", "file": "src/scope.mjs", "guard": "a bare 'No.' answers the clause before it",
      "find": "if (cl.negated && (!cl.aboutSomething || words(cl.text).some(w => ADDITIVE.has(w)))) {", "replace": "if (false) {" },
    { "id": "scope-negated-term", "file": "src/scope.mjs", "guard": "a term in a refusing clause is contradicted",
      "find": "if (clauseRefuses(m.ci)) { contradicted = true; continue }", "replace": "if (false) { contradicted = true; continue }" },
    { "id": "scope-hedged-consent", "file": "src/scope.mjs", "guard": "questions, conditionals and reported speech are not consent",
      "find": "if (sentences[cl.sentence].hedged) {", "replace": "if (false) {" },
    { "id": "scope-hedged-term", "file": "src/scope.mjs", "guard": "a term only said in a hedged sentence is missing",
      "find": "if (sentences[clauses[m.ci].sentence].hedged) {", "replace": "if (false) {" },
    { "id": "scope-first-person", "file": "src/scope.mjs", "guard": "consent must be first person",
      "find": "if (w[i] !== 'i' && w[i] !== 'we') continue", "replace": "" },
    { "id": "scope-ambiguous-alias", "file": "src/scope.mjs", "guard": "one stretch of speech never satisfies two terms",
      "find": "if (m.ambiguous) {", "replace": "if (false) {" },
    { "id": "scope-territory-compound", "file": "src/scope.mjs", "guard": "a place inside a longer place name is not that place",
      "find": "if (TERRITORY_BLOCKED_BEFORE.has(before) || TERRITORY_BLOCKED_AFTER.has(after)) ok = false", "replace": "" },
    { "id": "scope-unchecked", "file": "src/scope.mjs", "guard": "validity and ceiling are listed as unchecked",
      "find": "const unchecked = ['validity', 'ceiling']", "replace": "const unchecked = []" },

    { "id": "consent-transcript-is-clip", "file": "src/consent.mjs", "guard": "the consent clip itself is never read as its transcript",
      "find": "if (sameResource(u.href, sourceUrl)) throw", "replace": "if (false) throw" },
    { "id": "consent-transcript-content-type", "file": "src/consent.mjs", "guard": "a transcript link must be text or JSON",
      "find": "if (!TEXT_TYPES.test(type)) {", "replace": "if (false) {" },
    { "id": "consent-text-mixed-with-link", "file": "src/consent.mjs", "guard": "transcript text mixed with a link is refused",
      "find": "if (transcript && hasLink(transcript) && !looksLikeUrlOnly(transcript)) throw", "replace": "if (false) throw" },

    { "id": "execute-input-echo", "file": "src/execute.mjs", "guard": "a render's own input is never released as its output",
      "find": " && !inputs.has(pathKey(n)) ? n : null", "replace": " ? n : null" },
    { "id": "execute-input-echo-query", "file": "src/execute.mjs", "guard": "an input echoed back with a query string is still the input",
      "find": "!inputs.has(pathKey(n))", "replace": "!inputs.has(n)" },
    { "id": "execute-nested-inputs", "file": "src/execute.mjs", "guard": "inputs nested inside objects are collected",
      "find": "for (const v of Array.isArray(value) ? value : Object.values(value)) collectInputUrls(v, out, seen)", "replace": "if (Array.isArray(value)) for (const v of value) collectInputUrls(v, out, seen)" },
    { "id": "execute-job-inputs", "file": "src/execute.mjs", "guard": "a polled job's own source and inputs are never its output",
      "find": "const inputs = [...collectInputUrls(inputUrls), ...collectInputUrls(s.source_url), ...collectInputUrls(s.inputs)]", "replace": "const inputs = [...collectInputUrls(inputUrls)]" },
    { "id": "execute-queued-text-scanned", "file": "src/execute.mjs", "guard": "a queued text-only reply is polled, never scanned for a URL",
      "find": "const queued = IN_PROGRESS.has(status) || (jobId && !hasStructuredUrl(structured))", "replace": "const queued = IN_PROGRESS.has(status)" },
    { "id": "execute-no-media", "file": "src/execute.mjs", "guard": "a finished job with no media URL is an error",
      "find": "if (!url) throw new RenderError(`job ${jobId} finished without a media URL`", "replace": "if (false) throw new RenderError(`job ${jobId} finished without a media URL`" },
    { "id": "execute-unknown-status", "file": "src/execute.mjs", "guard": "an unrecognised job status stops the render",
      "find": "if (!IN_PROGRESS.has(status)) {", "replace": "if (false) {" },
    { "id": "execute-timeout-may-have-started", "file": "src/execute.mjs", "guard": "a call that got no answer may still be rendering",
      "find": "{ kind: timedOut ? 'timeout' : 'tool', jobId: null, mayHaveStarted: true }", "replace": "{ kind: timedOut ? 'timeout' : 'tool', jobId: null, mayHaveStarted: false }" },
    { "id": "execute-job-id-shape", "file": "src/execute.mjs", "guard": "a malformed job id is refused",
      "find": "if (typeof given === 'string' && JOB_ID.test(given)) return given", "replace": "if (typeof given === 'string') return given" },

    { "id": "dkg-seal-author", "file": "src/dkg.mjs", "guard": "a sealed asset must be authored by the expected address",
      "find": "if (expectAuthor && authorAddress !== String(expectAuthor).toLowerCase()) {\n      throw new DkgWriteError(`asset ${name} was sealed by", "replace": "if (false) {\n      throw new DkgWriteError(`asset ${name} was sealed by" },
    { "id": "dkg-resume-author", "file": "src/dkg.mjs", "guard": "resume refuses an asset recorded under another author",
      "find": "if (expectAuthor && authorAddress !== String(expectAuthor).toLowerCase()) {\n      refuse(", "replace": "if (false) {\n      refuse(" },
    { "id": "dkg-existing-name", "file": "src/dkg.mjs", "guard": "an existing asset name is refused without resume",
      "find": "if (existing && !resume) {", "replace": "if (false) {" },
    { "id": "dkg-resume-unknown-status", "file": "src/dkg.mjs", "guard": "resume refuses an asset in an unknown state",
      "find": "refuse(`the node reports status ${d.status ?? 'unknown'}`)", "replace": "return { name, ual: d.publishedUal ?? null, txHash: null, resumed: true }" },
    { "id": "dkg-tentative-ual", "file": "src/dkg.mjs", "guard": "a tentative UAL is not proof of a publish",
      "find": "const CONFIRMED_UAL = /^did:dkg:[a-z0-9]+:\\d+\\/(0x[0-9a-fA-F]{40})\\/(\\d+)$/", "replace": "const CONFIRMED_UAL = /^did:dkg:[a-z0-9]+:\\d+\\/(0x[0-9a-fA-F]{40})\\/t?([0-9a-f]+)$/" },
    { "id": "dkg-anchor-publisher", "file": "src/dkg.mjs", "guard": "a reconciled UAL must name the sealing author",
      "find": "if (author && publisher !== String(author).toLowerCase()) throw", "replace": "if (false) throw" },
    { "id": "dkg-anchor-merkle-root", "file": "src/dkg.mjs", "guard": "the published assertion must be the one sealed",
      "find": "if (merkleRoot && vm && vm !== bareHex(merkleRoot)) throw", "replace": "if (false) throw" },
    { "id": "dkg-anchor-confirmed-status", "file": "src/dkg.mjs", "guard": "a reconciled publish needs a confirmed _meta anchor",
      "find": "if (statuses.length !== 1 || statuses[0] !== 'confirmed') throw new Error(", "replace": "if (false) throw new Error(" },
    { "id": "dkg-anchor-assertion-graph", "file": "src/dkg.mjs", "guard": "a reconciled anchor must name the graph its UAL derives",
      "find": "if (graphs.length !== 1 || graphs[0] !== expectedGraph) throw new Error(", "replace": "if (false) throw new Error(" },
    { "id": "dkg-reconcile-descriptor-status", "file": "src/dkg.mjs", "guard": "a lost publish reconciles only a vm-confirmed descriptor",
      "find": "if (d?.status !== 'vm-confirmed') throw new Error(", "replace": "if (false) throw new Error(" },
    { "id": "dkg-response-cap", "file": "src/dkg.mjs", "guard": "a response body over the limit is not read",
      "find": "if (bytes > limit) {", "replace": "if (false) {" },

    { "id": "derivation-id-for-other-bytes", "file": "src/derivation.mjs", "guard": "a saved derivation id cannot be reused for other bytes",
      "find": "if (given && given.sha16 !== sha.slice(0, 16)) {", "replace": "if (false) {" },
    { "id": "derivation-reconcile-trusted", "file": "src/derivation.mjs", "guard": "only trusted edges account for billed renders",
      "find": "const trusted = derivations.filter(d => d?.trusted === true)", "replace": "const trusted = derivations" },
    { "id": "derivation-billed-usd", "file": "src/derivation.mjs", "guard": "a negative or non-finite billed amount is refused",
      "find": "if (typeof v === 'number' && Number.isFinite(v) && v >= 0) return v", "replace": "if (typeof v === 'number') return v" },

    { "id": "fetch-bytes-shared-budget", "file": "src/fetch-bytes.mjs", "guard": "the byte cap is one budget across retries",
      "find": "if (budget.left < 0) throw new FetchBytesError(", "replace": "if (false) throw new FetchBytesError(" },
    { "id": "fetch-bytes-undefined-cap", "file": "src/fetch-bytes.mjs", "guard": "an unusable maxBytes never switches the cap off",
      "find": "if (typeof v !== 'number' || !Number.isFinite(v) || v < min || (integer && !Number.isInteger(v))) {", "replace": "if (false) {" },
    { "id": "pending-protected-overwrite", "file": "src/pending.mjs", "guard": "a rerun cannot overwrite a submitted, rendered or recorded record",
      "find": "if (existing && !overwrite && PROTECTED_STATUSES.includes(existing.status)) throw", "replace": "if (false) throw" },
    { "id": "resolve-merged-view-missing", "file": "src/resolve.mjs", "guard": "on a node that holds a merged view, a view left out of every answer fails the read",
      "find": "const viewMissing = [...materialised].filter(", "replace": "const viewMissing = [].filter(" },
    { "id": "resolve-merged-view-probe-only-when-held", "file": "src/resolve.mjs", "guard": "a node that holds no merged view is not required to show one",
      "find": "const viewMissing = [...materialised].filter(", "replace": "const viewMissing = [...expectView].filter(" },
    { "id": "resolve-merged-view-probe-unanswered", "file": "src/resolve.mjs", "guard": "a merged-view probe that never answers fails the read",
      "find": "for (const cg of probesUnanswered) failures.push(", "replace": "for (const cg of []) failures.push(" }
  ]
}
